# Test profile placeholder.
#
# Nothing test-specific needs overriding yet — Slice 1 has no datasource and
# no environment-sensitive behavior. This file exists now so the profile
# structure (default/local, test, production) is established before Slice 2
# needs it (e.g. pointing tests at a throwaway database rather than the
# shared one, matching the Node reference's own e2e-database isolation
# pattern). Activate explicitly with -Dspring.profiles.active=test; nothing
# activates it implicitly.
