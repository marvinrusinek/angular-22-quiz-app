<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.1</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.quizbackend</groupId>
	<artifactId>quiz-backend</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>quiz-backend</name>
	<description>Spring Boot migration of the Interview backend</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>21</java.version>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>

		<!-- Slice 2: minimum JPA/PostgreSQL infrastructure. Spring Data JPA
		     (Hibernate) for entity mapping against the EXISTING Neon schema
		     (ddl-auto=validate — never create/update), and the PostgreSQL JDBC
		     driver itself. No Flyway/Liquibase/Lombok/MapStruct — the existing
		     schema is canonical and none of those are needed to read it. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>

		<!-- Testcontainers hardening slice: a disposable real-PostgreSQL
		     integration-test layer, so Interview persistence mapping (composite
		     keys, generated columns, partial indexes — none of which H2 models
		     faithfully) can be developed against genuine Postgres semantics
		     instead of only against live Neon. TEST SCOPE ONLY — see the JAR
		     hygiene check in the Testcontainers slice report for proof none of
		     this reaches the production artifact. Versions are managed by the
		     spring-boot-starter-parent BOM (spring-boot-dependencies imports
		     org.testcontainers:testcontainers-bom:2.0.5), not pinned here.
		     NOTE: Testcontainers 2.x renamed these artifacts from the widely
		     documented Testcontainers 1.x coordinates (org.testcontainers:
		     junit-jupiter / org.testcontainers:postgresql) to
		     testcontainers-junit-jupiter / testcontainers-postgresql —
		     confirmed against the actual resolved testcontainers-bom, not
		     assumed from older documentation. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-testcontainers</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-postgresql</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<!-- Runs *IT.java classes at the integration-test/verify phases,
			     NOT at test/package — so `mvnw.cmd test` and `mvnw.cmd clean
			     package` stay fast and Docker-independent, and the disposable
			     PostgreSQL Testcontainers suite only runs on an explicit
			     `mvnw.cmd verify`. Version managed by spring-boot-starter-parent
			     (pluginManagement), not pinned here. -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-failsafe-plugin</artifactId>
				<executions>
					<execution>
						<goals>
							<goal>integration-test</goal>
							<goal>verify</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>
