package com.quizbackend.interview;

/** Port of the Node reference's {@code GeneratedOptionSnapshot}. ANSWER KEY — backend only. */
public record GeneratedOptionSnapshot(int optionId, String optionText, int displayOrder, boolean isCorrect) {
}
