{
  "quizzes": [
    {
      "quizId": "typescript",
      "milestone": "TypeScript",
      "summary": "TypeScript makes it easier to read and debug JavaScript code.",
      "image": "https://raw.githubusercontent.com/marvinrusinek/codelab-angular-10-quiz-app/quiz/codelab-master/apps/playground/src/assets/images/ts.png",
      "difficulty": "beginner",
      "facts": [
        "TypeScript compiles down to plain JavaScript, so the types are erased at build time and the output runs anywhere JavaScript runs.",
        "Type inference means TypeScript often knows a variable's type from its initial value, so you don't have to annotate everything.",
        "Strict mode catches whole classes of bugs at compile time, such as using a value that might be null or undefined."
      ],
      "questions": [
        {
          "questionText": "Which of the following does TypeScript use to specify types?",
          "options": [
            {
              "text": ":",
              "correct": true
            },
            {
              "text": ";"
            },
            {
              "text": "!"
            },
            {
              "text": "&"
            }
          ],
          "explanation": "TS uses a colon (:) to separate the property name from the property type"
        },
        {
          "questionText": "Which of the following is NOT a built-in TypeScript type?",
          "options": [
            {
              "text": "number"
            },
            {
              "text": "string"
            },
            {
              "text": "boolean"
            },
            {
              "text": "integer",
              "correct": true
            }
          ],
          "explanation": "TypeScript has no separate integer type — whole numbers are represented by number. string and boolean are also built-in primitive types, so integer is the odd one out."
        },
        {
          "questionText": "How can we specify properties and methods for an object in TypeScript?",
          "options": [
            {
              "text": "Use classes."
            },
            {
              "text": "Use interfaces.",
              "correct": true
            },
            {
              "text": "Use enums."
            },
            {
              "text": "Use async/await."
            }
          ],
          "explanation": "interfaces are typically used to list the properties and methods for an object"
        },
        {
          "questionText": "How else can Array<number> be written in TypeScript?",
          "options": [
            {
              "text": "@number"
            },
            {
              "text": "number[]",
              "correct": true
            },
            {
              "text": "number!"
            },
            {
              "text": "number?"
            }
          ],
          "explanation": "number[] is another way of writing Array<number> in TypeScript"
        },
        {
          "questionText": "In which of these does a class take parameters?",
          "options": [
            {
              "text": "constructor",
              "correct": true
            },
            {
              "text": "destructor"
            },
            {
              "text": "import"
            },
            {
              "text": "subscribe"
            }
          ],
          "explanation": "a constructor is used by a class to take in parameters"
        },
        {
          "questionText": "Which is NOT an access modifier?",
          "options": [
            {
              "text": "private"
            },
            {
              "text": "protected"
            },
            {
              "text": "public"
            },
            {
              "text": "async",
              "correct": true
            }
          ],
          "explanation": "async is not used as an access modifier type in TypeScript"
        },
        {
          "questionText": "Which keyword allows us to share information between files in TypeScript?",
          "options": [
            {
              "text": "import"
            },
            {
              "text": "export",
              "correct": true
            },
            {
              "text": "async"
            },
            {
              "text": "constructor"
            }
          ],
          "explanation": "the export keyword allows for the information to be transmitted between files"
        },
        {
          "questionText": "Which is an array method to generate a new array based on a condition?",
          "options": [
            {
              "text": "filter",
              "correct": true
            },
            {
              "text": "map"
            },
            {
              "text": "async"
            },
            {
              "text": "enum"
            }
          ],
          "explanation": "filter is a method used to conditionally create a new array"
        },
        {
          "questionText": "How is a property accessible within a class?",
          "options": [
            {
              "text": "Using this.propertyName",
              "correct": true
            },
            {
              "text": "Accessors"
            },
            {
              "text": "Destructuring"
            },
            {
              "text": "Arrow function"
            }
          ],
          "explanation": "this.propertyName is the way to access a specific property within a class"
        },
        {
          "questionText": "How do you declare a variable that can hold either a string or a number?",
          "options": [
            {
              "text": "let value: string | number;",
              "correct": true
            },
            {
              "text": "let value: string & number;"
            },
            {
              "text": "let value: string, number;"
            },
            {
              "text": "let value: any[];"
            }
          ],
          "explanation": "a union type, string | number, lets a value be one of several types — here a string or a number. The & is an intersection (requiring both at once, impossible for these primitives), the comma form is invalid, and any[] is an array of any rather than a string-or-number value."
        }
      ]
    },
    {
      "quizId": "create-first-app",
      "milestone": "Creating your first app",
      "summary": "Angular allows us to create an app that contains components and modules as well as a system for bootstrapping the app.",
      "image": "https://raw.githubusercontent.com/marvinrusinek/codelab-angular-10-quiz-app/quiz/codelab-master/apps/playground/src/assets/images/first_app.png",
      "difficulty": "beginner",
      "facts": [
        "A modern Angular app bootstraps a single root standalone component with bootstrapApplication — no root NgModule required.",
        "Every component pairs a TypeScript class for logic with a template for the view, wired together by the @Component decorator.",
        "The Angular CLI scaffolds a runnable app in seconds, complete with a dev server, unit testing, and a production build."
      ],
      "questions": [
        {
          "questionText": "Which of the following are true statements about Angular?",
          "options": [
            {
              "text": "Angular is a development platform."
            },
            {
              "text": "Angular minimizes much of the code you would have to write."
            },
            {
              "text": "Angular extends HTML's syntax to define your HTML components."
            },
            {
              "text": "All of the above.",
              "correct": true
            }
          ],
          "explanation": "all of the above are true statements about Angular"
        },
        {
          "questionText": "Which of the following can be added to an Angular class?",
          "options": [
            {
              "text": "Properties and methods",
              "correct": true
            },
            {
              "text": "Imports and exports"
            },
            {
              "text": "Template variables"
            },
            {
              "text": "Styles"
            }
          ],
          "explanation": "properties and methods can be added to a class in Angular"
        },
        {
          "questionText": "How is a class adorned in Angular?",
          "options": [
            {
              "text": "Using the @Component decorator.",
              "correct": true
            },
            {
              "text": "Using the @Injectable decorator."
            },
            {
              "text": "Using the @Input decorator."
            },
            {
              "text": "Using the @Output decorator."
            }
          ],
          "explanation": "a class is adorned with the @Component decorator in Angular"
        },
        {
          "questionText": "What is the purpose of a decorator in TypeScript?",
          "options": [
            {
              "text": "To attach metadata to a class, function, property or variable.",
              "correct": true
            },
            {
              "text": "To add properties and methods to the class."
            },
            {
              "text": "To minimize the amount of code you will write."
            },
            {
              "text": "To perform databinding."
            }
          ],
          "explanation": "a decorator attaches metadata to a class, function, property or variable"
        },
        {
          "questionText": "Which defines the location of a component?",
          "options": [
            {
              "text": "decorator"
            },
            {
              "text": "selector",
              "correct": true
            },
            {
              "text": "template"
            },
            {
              "text": "templateUrl"
            }
          ],
          "explanation": "a selector defines the location of a component in Angular"
        },
        {
          "questionText": "Which defines the HTML code that the component generates?",
          "options": [
            {
              "text": "decorator"
            },
            {
              "text": "selector"
            },
            {
              "text": "template",
              "correct": true
            },
            {
              "text": "templateUrl"
            }
          ],
          "explanation": "a template contains the HTML code that a component generates"
        },
        {
          "questionText": "In Angular 22, components are standalone by default. How do you make another component, directive, or pipe available to a component's template?",
          "options": [
            {
              "text": "Add it to the component's imports array.",
              "correct": true
            },
            {
              "text": "List it in index.html."
            },
            {
              "text": "Nothing — everything is available automatically."
            },
            {
              "text": "Register it as a provider."
            }
          ],
          "explanation": "A standalone component declares its template dependencies in its own imports array. Nothing is global by default, and providers is for injectable services, not template dependencies."
        },
        {
          "questionText": "Which decorator marks a class as an Angular component and supplies its metadata (selector, template, styles)?",
          "options": [
            {
              "text": "@Component",
              "correct": true
            },
            {
              "text": "@NgModule"
            },
            {
              "text": "@Injectable"
            },
            {
              "text": "@Directive"
            }
          ],
          "explanation": "the @Component decorator marks a class as a component and provides its selector, template, and styles (and, by default in modern Angular, standalone: true). @NgModule declares the older module type, @Injectable marks a service, and @Directive marks a directive."
        },
        {
          "questionText": "In modern Angular, standalone components are the default. What does that mean?",
          "options": [
            {
              "text": "A component manages its own dependencies via imports and does not need to be declared in an NgModule.",
              "correct": true
            },
            {
              "text": "Every component must still be listed in an NgModule's declarations."
            },
            {
              "text": "Components can no longer use other components."
            },
            {
              "text": "Components are compiled without a template."
            }
          ],
          "explanation": "standalone components (the default in new Angular apps) declare their own dependencies in imports and do not need an NgModule or its declarations array. They can still use other components by importing them, and they still have templates."
        },
        {
          "questionText": "How is a modern standalone Angular application bootstrapped?",
          "options": [
            {
              "text": "bootstrapApplication(AppComponent, appConfig);",
              "correct": true
            },
            {
              "text": "platformBrowserDynamic().bootstrapModule(AppModule);"
            },
            {
              "text": "new AppComponent();"
            },
            {
              "text": "ng serve"
            }
          ],
          "explanation": "a standalone app starts with bootstrapApplication(AppComponent, appConfig) in main.ts, passing the root component and app-level providers. platformBrowserDynamic().bootstrapModule(AppModule) is the older NgModule bootstrap, you never construct a component with new, and ng serve is the CLI dev-server command."
        }
      ]
    },
    {
      "quizId": "templates",
      "milestone": "Templates",
      "summary": "Angular has a very expressive template system, which takes HTML as a base, and extends it with custom elements.",
      "image": "https://raw.githubusercontent.com/marvinrusinek/codelab-angular-10-quiz-app/quiz/codelab-master/apps/playground/src/assets/images/template.png",
      "difficulty": "beginner",
      "facts": [
        "Angular's built-in control flow — @if, @for, @switch — is part of the template syntax, so it needs no directive imports.",
        "The three core bindings are property binding [x], event binding (x), and two-way binding [(x)] (the \"banana in a box\").",
        "Template expressions are sandboxed from globals like window, which keeps templates safe, predictable, and easy to test."
      ],
      "questions": [
        {
          "questionText": "What syntax does Angular use for text interpolation — inserting a value into the template as text?",
          "options": [
            {
              "text": "Double curly braces: {{ value }}",
              "correct": true
            },
            {
              "text": "Backticks: `value`"
            },
            {
              "text": "Double brackets: [[ value ]]"
            },
            {
              "text": "Double parentheses: (( value ))"
            }
          ],
          "explanation": "Angular uses double curly braces, {{ expression }}, for text interpolation — it evaluates the expression and renders the result as text. Backticks are JavaScript template literals, not Angular template syntax, and the bracket and parenthesis forms are not interpolation."
        },
        {
          "questionText": "Which syntax binds a component value to an element's property, such as an image's src?",
          "options": [
            {
              "text": "[src]=\"imageUrl\"",
              "correct": true
            },
            {
              "text": "{{ src }}"
            },
            {
              "text": "(src)=\"imageUrl\""
            },
            {
              "text": "#src=\"imageUrl\""
            }
          ],
          "explanation": "property binding uses square brackets — [property]=\"expression\", e.g. [src]=\"imageUrl\" — to set a DOM property from a component value. Interpolation {{ }} inserts text, parentheses () are for event binding, and # declares a template reference variable."
        },
        {
          "questionText": "Which syntax handles a DOM event, such as a button click?",
          "options": [
            {
              "text": "(click)=\"onClick()\"",
              "correct": true
            },
            {
              "text": "[click]=\"onClick()\""
            },
            {
              "text": "{{ click }}"
            },
            {
              "text": "#click=\"onClick()\""
            }
          ],
          "explanation": "event binding wraps the event name in parentheses — (click)=\"onClick()\" — to call a handler when the event fires. Square brackets are property binding, {{ }} is interpolation, and # is a template reference variable."
        },
        {
          "questionText": "Which is a shortcut for toggling a CSS class based on a boolean property?",
          "options": [
            {
              "text": "[class.active]=\"isActive\"",
              "correct": true
            },
            {
              "text": "{{ active }}"
            },
            {
              "text": "(class.active)=\"isActive\""
            },
            {
              "text": "class$=\"isActive\""
            }
          ],
          "explanation": "class binding, [class.active]=\"isActive\", adds the class when the expression is truthy and removes it when falsy. Interpolation and event syntax do not toggle classes, and there is no class$ shorthand."
        },
        {
          "questionText": "What is the correct way to bind a style value in an Angular template?",
          "options": [
            {
              "text": "[style.color]=\"textColor\"",
              "correct": true
            },
            {
              "text": "{{ style.color }}"
            },
            {
              "text": "(style.color)=\"textColor\""
            },
            {
              "text": "style=\"{{ textColor }}\""
            }
          ],
          "explanation": "style binding uses [style.property]=\"expression\", e.g. [style.color]=\"textColor\", to set a style from a component value. Interpolation and event syntax do not bind styles, and mixing a static style attribute with interpolation is not the idiomatic binding form."
        },
        {
          "questionText": "How do you get a reference to an element or component from within the template?",
          "options": [
            {
              "text": "Mark it with a template reference variable: #name",
              "correct": true
            },
            {
              "text": "Use attribute binding: [attr]=\"name\""
            },
            {
              "text": "Use interpolation: {{ name }}"
            },
            {
              "text": "Use an event: (name)"
            }
          ],
          "explanation": "a template reference variable, declared with #name, gives you a handle to that element or component elsewhere in the same template. Attribute binding, interpolation, and event binding do not create a reference you can read."
        },
        {
          "questionText": "In modern Angular (17+), which built-in block conditionally includes content in the template?",
          "options": [
            {
              "text": "@if (condition) { ... }",
              "correct": true
            },
            {
              "text": "@when (condition) { ... }"
            },
            {
              "text": "[visible]=\"condition\""
            },
            {
              "text": "{{ if condition }}"
            }
          ],
          "explanation": "Angular's built-in control flow uses @if (condition) { ... } (with optional @else) to include or remove content, replacing the older *ngIf structural directive. There is no @when block, and the other forms are not control flow."
        },
        {
          "questionText": "In modern Angular, which built-in block renders a list, and what must it include for good performance?",
          "options": [
            {
              "text": "@for (item of items; track item.id) { ... }",
              "correct": true
            },
            {
              "text": "@each (item of items) { ... }"
            },
            {
              "text": "@for (item of items) { ... } with no track"
            },
            {
              "text": "[repeat]=\"items\""
            }
          ],
          "explanation": "the built-in @for block loops over a collection and requires a track expression — @for (item of items; track item.id) { ... } — so Angular can reuse DOM nodes by identity instead of rebuilding the whole list. @each does not exist, @for without track is invalid, and there is no [repeat] binding."
        },
        {
          "questionText": "Which of the following are valid Angular template bindings? Select all that apply.",
          "options": [
            {
              "text": "Property binding: [disabled]=\"isDisabled\"",
              "correct": true
            },
            {
              "text": "Event binding: (submit)=\"onSubmit()\"",
              "correct": true
            },
            {
              "text": "Two-way binding: [(ngModel)]=\"name\"",
              "correct": true
            },
            {
              "text": "An event mixed with interpolation: (count)=\"{{ n }}\""
            }
          ],
          "explanation": "property binding [ ], event binding ( ), and two-way binding [( )] (the \"banana in a box\") are all valid. Mixing an event binding with interpolation, (count)=\"{{ n }}\", is not valid Angular syntax."
        },
        {
          "questionText": "What is Angular's syntax for handling a keyboard shortcut like Ctrl+Enter?",
          "options": [
            {
              "text": "(keydown.control.enter)=\"onShortcut()\"",
              "correct": true
            },
            {
              "text": "(keydown)=\"ctrl+enter\""
            },
            {
              "text": "[keydown.control.enter]=\"onShortcut()\""
            },
            {
              "text": "{{ keydown.control.enter }}"
            }
          ],
          "explanation": "Angular lets you bind a specific key combination right in the event name — (keydown.control.enter)=\"onShortcut()\" — so the handler runs only for that combination. The other forms are not valid event-binding syntax for key combinations."
        }
      ]
    },
    {
      "quizId": "dependency-injection",
      "milestone": "Dependency Injection",
      "summary": "Dependency Injection is a way of providing dependencies in your code instead of hard-coding them.",
      "image": "https://raw.githubusercontent.com/marvinrusinek/codelab-angular-10-quiz-app/quiz/codelab-master/apps/playground/src/assets/images/DIDiagram.png",
      "difficulty": "intermediate",
      "facts": [
        "Angular's injector is hierarchical — a dependency is resolved by walking up from the component toward the root injector.",
        "providedIn: 'root' makes a service tree-shakable: if nothing injects it, it is dropped from the production bundle.",
        "The inject() function retrieves dependencies outside a constructor, such as in field initializers or reusable functions."
      ],
      "questions": [
        {
          "questionText": "What is Dependency Injection (DI) in Angular?",
          "options": [
            {
              "text": "A design pattern where a class receives its dependencies from Angular's injector instead of creating them itself.",
              "correct": true
            },
            {
              "text": "A method for creating animations."
            },
            {
              "text": "A pattern for storing data in the browser."
            },
            {
              "text": "A technique for defining routes."
            }
          ],
          "explanation": "dependency injection is a pattern where a class declares what it needs and Angular's injector supplies those dependencies, instead of the class constructing them with new. It is not about animations, browser storage, or routing."
        },
        {
          "questionText": "Which of the following benefit from dependency injection?",
          "options": [
            {
              "text": "Maintainability"
            },
            {
              "text": "Testability"
            },
            {
              "text": "Loose coupling"
            },
            {
              "text": "All of the above.",
              "correct": true
            }
          ],
          "explanation": "DI improves maintainability and loose coupling by keeping classes independent of how their dependencies are built, and it makes testing easier because dependencies can be swapped for mocks — so all of the above apply."
        },
        {
          "questionText": "Which of the following statements are true about Dependency Injection (DI)? Select all that apply.",
          "options": [
            {
              "text": "DI is a technique where a class receives its dependencies from external sources rather than creating them itself.",
              "correct": true
            },
            {
              "text": "DI always leads to significant performance improvements in applications."
            },
            {
              "text": "DI helps reduce coupling between classes, making the code more modular.",
              "correct": true
            },
            {
              "text": "DI makes it easy to provide a mock or fake dependency in unit tests.",
              "correct": true
            }
          ],
          "explanation": "DI has a class receive its dependencies from outside, which reduces coupling and makes the code modular, and it lets tests supply a mock in place of the real dependency. It is about design and testability, not a guaranteed performance boost — that claim is the incorrect option."
        },
        {
          "questionText": "In modern Angular, how can a class obtain one of its dependencies?",
          "options": [
            {
              "text": "Through constructor injection, or by calling the inject() function.",
              "correct": true
            },
            {
              "text": "Only by importing the dependency's file."
            },
            {
              "text": "By reading it from the global window object."
            },
            {
              "text": "By creating it with new inside the class."
            }
          ],
          "explanation": "Angular supplies a dependency either as a constructor parameter or through the inject() function (e.g. private http = inject(HttpClient)), both resolved by the injector. Importing a file only makes the type available, globals bypass DI, and using new defeats the purpose of DI entirely."
        },
        {
          "questionText": "How do you register a service so it is available app-wide as a single shared instance?",
          "options": [
            {
              "text": "@Injectable({ providedIn: 'root' })",
              "correct": true
            },
            {
              "text": "@Component({ providedIn: 'root' })"
            },
            {
              "text": "@Injectable({ singleton: true })"
            },
            {
              "text": "export const service = new MyService();"
            }
          ],
          "explanation": "@Injectable({ providedIn: 'root' }) registers the service with the root injector, giving the whole app one shared (singleton) instance that is also tree-shakable if unused. @Component is for components, there is no singleton option, and exporting a new instance bypasses Angular's DI."
        },
        {
          "questionText": "What does calling inject(HttpClient) do inside a component or service?",
          "options": [
            {
              "text": "It returns the HttpClient instance from the current injector, without needing a constructor parameter.",
              "correct": true
            },
            {
              "text": "It creates a brand-new HttpClient with new each time."
            },
            {
              "text": "It sends an HTTP request immediately."
            },
            {
              "text": "It registers HttpClient as a provider."
            }
          ],
          "explanation": "inject(HttpClient) asks the current injector for the existing HttpClient instance and returns it, which is handy in field initializers, functions, and base classes. It does not construct a new instance, make a request, or register a provider."
        },
        {
          "questionText": "Why is DI preferred over creating a dependency with new directly inside a class?",
          "options": [
            {
              "text": "It decouples the class from a concrete implementation and lets you swap or mock the dependency, especially in tests.",
              "correct": true
            },
            {
              "text": "It makes the application bundle larger on purpose."
            },
            {
              "text": "It prevents the class from ever being reused."
            },
            {
              "text": "It disables change detection for the class."
            }
          ],
          "explanation": "obtaining a dependency through DI means the class depends on an abstraction the injector provides, so you can reconfigure or mock it (invaluable for unit tests) without editing the class. Using new hard-wires a specific implementation; DI does not bloat bundles, block reuse, or affect change detection."
        }
      ]
    },
    {
      "quizId": "component-tree",
      "milestone": "Component Trees",
      "summary": "An Angular application can be thought of as a tree of reusable components.",
      "image": "https://raw.githubusercontent.com/marvinrusinek/codelab-angular-10-quiz-app/quiz/codelab-master/apps/playground/src/assets/images/tree.png",
      "difficulty": "intermediate",
      "facts": [
        "Data flows down through inputs and events flow up through outputs — a one-way pattern that keeps interactions predictable.",
        "Components that are not directly related usually communicate through a shared service holding a signal or a Subject.",
        "Signal inputs (input()) and outputs (output()) are the modern, type-safe alternatives to the @Input and @Output decorators."
      ],
      "questions": [
        {
          "questionText": "What is a component tree in Angular?",
          "options": [
            {
              "text": "A visual representation of how components are nested within an application",
              "correct": true
            },
            {
              "text": "A data structure used to store component metadata"
            },
            {
              "text": "A tool for debugging Angular applications"
            },
            {
              "text": "A lifecycle hook used to initialize components"
            }
          ],
          "explanation": "a component tree in Angular shows how components are nested and related"
        },
        {
          "questionText": "Why is understanding the component tree important in Angular development?",
          "options": [
            {
              "text": "It helps in managing application styles."
            },
            {
              "text": "It assists in debugging and understanding component interactions.",
              "correct": true
            },
            {
              "text": "It is used to configure Angular modules."
            },
            {
              "text": "It determines the order of service injection."
            }
          ],
          "explanation": "knowing the component tree aids in troubleshooting and clarifying how components relate to each other"
        },
        {
          "questionText": "How does a parent component pass data down to a child component?",
          "options": [
            {
              "text": "By binding to the child's input — an @Input() property or an input() signal — e.g. <app-child [value]=\"data\">.",
              "correct": true
            },
            {
              "text": "By reaching into the child and calling a method on it."
            },
            {
              "text": "Through a shared global variable on window."
            },
            {
              "text": "By emitting an event from the parent."
            }
          ],
          "explanation": "a parent passes data down by binding to the child's input — an @Input() property or a signal input() — like <app-child [value]=\"data\">. It does not reach into the child to call methods or use globals, and emitting events is how a child sends data up, not how a parent sends data down."
        },
        {
          "questionText": "How does a child component send data or an event up to its parent?",
          "options": [
            {
              "text": "By emitting through an @Output() (or output()) that the parent listens to with (event)=\"handler($event)\".",
              "correct": true
            },
            {
              "text": "By adding an @Input() to the parent."
            },
            {
              "text": "By directly reassigning the parent's fields."
            },
            {
              "text": "By using string interpolation."
            }
          ],
          "explanation": "a child sends data up by emitting through an @Output() EventEmitter (or the output() function), and the parent listens with (event)=\"handler($event)\". @Input() is for receiving data from the parent, and a child should never reach into or mutate the parent directly."
        },
        {
          "questionText": "In a standalone Angular app, how do you make one component available to render inside another?",
          "options": [
            {
              "text": "Add it to the host component's imports array, then use its selector tag.",
              "correct": true
            },
            {
              "text": "Declare both in the same NgModule."
            },
            {
              "text": "Export it from a service."
            },
            {
              "text": "Nothing — every component is globally available."
            }
          ],
          "explanation": "standalone components list what they use in their own imports array; once a child is imported you render it with its selector, e.g. <app-child>. Declaring components in an NgModule is the older pattern, components are not exported from services, and they are not globally available."
        },
        {
          "questionText": "Which statements about component communication in Angular are correct? Select all that apply.",
          "options": [
            {
              "text": "@Input() (or input()) receives data from the parent.",
              "correct": true
            },
            {
              "text": "@Output() (or output()) emits events up to the parent.",
              "correct": true
            },
            {
              "text": "A shared service (with a signal or a Subject) can share state between unrelated components.",
              "correct": true
            },
            {
              "text": "A child component can directly reassign its parent's private fields."
            }
          ],
          "explanation": "@Input()/input() receive data from a parent, @Output()/output() emit events up to a parent, and a shared service (holding a signal or a Subject) is the standard way to share state between components that are not directly related. A child cannot and should not directly reassign a parent's private fields."
        },
        {
          "questionText": "How can a parent get a reference to a child component instance in its own class?",
          "options": [
            {
              "text": "With a view query — @ViewChild(ChildComponent) or the viewChild() signal.",
              "correct": true
            },
            {
              "text": "With an @Input() on the parent."
            },
            {
              "text": "With an @Output() on the parent."
            },
            {
              "text": "With string interpolation."
            }
          ],
          "explanation": "to access a child component instance from the parent's class you use a view query — @ViewChild(ChildComponent) or the modern viewChild() signal — which resolves once the view has initialized. @Input/@Output handle data binding rather than direct references, and interpolation only renders text."
        }
      ]
    },
    {
      "quizId": "router",
      "milestone": "Angular Router",
      "summary": "Angular Router helps developers build Single Page Applications with multiple views and allow navigation between those views.",
      "image": "https://raw.githubusercontent.com/marvinrusinek/codelab-angular-10-quiz-app/quiz/codelab-master/apps/playground/src/assets/images/router.png",
      "difficulty": "intermediate",
      "facts": [
        "Lazy routes (loadComponent / loadChildren) put a feature in its own bundle that downloads only when the route is visited.",
        "Route guards such as canActivate can allow, block, or redirect navigation — handy for auth and unsaved-changes checks.",
        "withComponentInputBinding() binds route parameters directly to component inputs, so you can skip manual ActivatedRoute reads."
      ],
      "questions": [
        {
          "questionText": "Which Angular feature maps URLs to the views (components) of a single-page app?",
          "options": [
            {
              "text": "The Router",
              "correct": true
            },
            {
              "text": "Forms"
            },
            {
              "text": "Templates"
            },
            {
              "text": "HttpClient"
            }
          ],
          "explanation": "the Angular Router maps each URL to a component, letting a single-page app switch views without a full page reload. Forms handle user input, templates render markup, and HttpClient talks to servers."
        },
        {
          "questionText": "How are routes defined in Angular?",
          "options": [
            {
              "text": "As an array of route objects, each mapping a path to a component, e.g. { path: 'home', component: HomeComponent }.",
              "correct": true
            },
            {
              "text": "In tsconfig.json."
            },
            {
              "text": "Inside index.html."
            },
            {
              "text": "As CSS classes in the stylesheet."
            }
          ],
          "explanation": "routes are an array of Route objects, each mapping a URL path to the component to display, e.g. { path: 'home', component: HomeComponent }. They are not configured in tsconfig.json, index.html, or CSS."
        },
        {
          "questionText": "In a standalone Angular app, how do you register the router with your routes?",
          "options": [
            {
              "text": "Add provideRouter(routes) to the providers in bootstrapApplication (or appConfig).",
              "correct": true
            },
            {
              "text": "Call RouterModule.forRoot(routes) inside every component."
            },
            {
              "text": "List the routes in angular.json."
            },
            {
              "text": "Assign the routes to a global window variable."
            }
          ],
          "explanation": "standalone apps set up routing by adding provideRouter(routes) to the application's providers (in bootstrapApplication or the appConfig). RouterModule.forRoot(routes) is the older NgModule approach, and routes do not live in angular.json or a global variable."
        },
        {
          "questionText": "What element tells the router where to display the component for the current URL?",
          "options": [
            {
              "text": "<router-outlet>",
              "correct": true
            },
            {
              "text": "routerLink"
            },
            {
              "text": "navigateByUrl"
            },
            {
              "text": "<ng-template>"
            }
          ],
          "explanation": "the <router-outlet> element marks the spot in a template where the router renders the component matched to the current URL. routerLink creates links, navigateByUrl navigates from code, and <ng-template> holds deferred template content."
        },
        {
          "questionText": "Which directive creates a clickable navigation link in a template?",
          "options": [
            {
              "text": "routerLink",
              "correct": true
            },
            {
              "text": "router-outlet"
            },
            {
              "text": "provideRouter"
            },
            {
              "text": "ActivatedRoute"
            }
          ],
          "explanation": "the routerLink directive turns an element into a navigation link, e.g. <a [routerLink]=\"['/home']\">. router-outlet is where views render, provideRouter registers routing, and ActivatedRoute reads route data."
        },
        {
          "questionText": "How do you lazy-load a feature route so its code stays out of the initial bundle?",
          "options": [
            {
              "text": "Use loadComponent (or loadChildren) on the route so the code loads on demand.",
              "correct": true
            },
            {
              "text": "Set eager: true on the route."
            },
            {
              "text": "Import the component eagerly in main.ts."
            },
            {
              "text": "Add the component to every other component's imports."
            }
          ],
          "explanation": "a route can use loadComponent: () => import('./x').then(m => m.X) (or loadChildren) so Angular splits that feature into a separate chunk loaded only when the route is visited, shrinking the initial bundle. Importing it eagerly in main.ts or everywhere pulls it into the initial bundle instead."
        },
        {
          "questionText": "How can a routed component read a URL parameter such as :id?",
          "options": [
            {
              "text": "Inject ActivatedRoute and read its paramMap, or bind the param to a component input via withComponentInputBinding().",
              "correct": true
            },
            {
              "text": "Read it from localStorage."
            },
            {
              "text": "Parse window.location manually in the constructor."
            },
            {
              "text": "It is passed automatically as a global variable."
            }
          ],
          "explanation": "you read route parameters by injecting ActivatedRoute and reading its paramMap, or by enabling withComponentInputBinding() so the router binds :id directly to a matching component input. localStorage, manual window.location parsing, and globals are not how Angular exposes route params."
        }
      ]
    },
    {
      "quizId": "material",
      "milestone": "Angular Material",
      "summary": "Angular Material provides a set of Material Design components that are consistent, versatile and look great on mobile devices.",
      "image": "https://raw.githubusercontent.com/marvinrusinek/codelab-angular-10-quiz-app/quiz/codelab-master/apps/playground/src/assets/images/material.png",
      "difficulty": "intermediate",
      "facts": [
        "Angular Material is built on the CDK (Component Dev Kit), which also ships unstyled behaviors you can use on their own.",
        "Material components include accessibility out of the box — ARIA roles, keyboard support, and focus management.",
        "A single Material theme defines your color palette and typography and applies it consistently across every component."
      ],
      "questions": [
        {
          "questionText": "In a standalone component, how do you make an Angular Material button (<button mat-button>) available?",
          "options": [
            {
              "text": "Import MatButtonModule into the component's imports array.",
              "correct": true
            },
            {
              "text": "Add it to an NgModule's declarations."
            },
            {
              "text": "Register it as a provider."
            },
            {
              "text": "Nothing — Material components are available everywhere by default."
            }
          ],
          "explanation": "Angular Material ships as modules; in a standalone component you import the specific module (e.g. MatButtonModule) into the component's imports array and then use its components in the template. Material goes in imports, not declarations or providers, and is not globally available."
        },
        {
          "questionText": "Which of the following are true about Angular Material components? Select all that apply.",
          "options": [
            {
              "text": "They follow Material Design for a consistent look.",
              "correct": true
            },
            {
              "text": "They can be themed with a custom color palette.",
              "correct": true
            },
            {
              "text": "They include built-in accessibility (ARIA roles, keyboard support).",
              "correct": true
            },
            {
              "text": "They only work in apps that still use NgModules."
            }
          ],
          "explanation": "Angular Material components follow Material Design for consistency, support theming with custom palettes, and ship with built-in accessibility such as ARIA roles and keyboard handling. They work in modern standalone apps too — the NgModule-only claim is the incorrect option."
        },
        {
          "questionText": "Where are Angular Material components used?",
          "options": [
            {
              "text": "In the template (HTML).",
              "correct": true
            },
            {
              "text": "In the TypeScript class file."
            },
            {
              "text": "In the CSS file."
            },
            {
              "text": "In angular.json."
            }
          ],
          "explanation": "Angular Material components are elements you place in the template, such as <mat-card> or <button mat-button>. They are not written in the TypeScript class, the stylesheet, or angular.json."
        },
        {
          "questionText": "How do you apply a consistent color scheme across your Angular Material components?",
          "options": [
            {
              "text": "Define a Material theme (with color palettes) and apply it globally.",
              "correct": true
            },
            {
              "text": "Set the color inline on every single component."
            },
            {
              "text": "Edit the Angular Material library's source code."
            },
            {
              "text": "Hard-code colors in a separate CSS file for each component."
            }
          ],
          "explanation": "Angular Material uses theming: you define a theme with primary/accent/warn palettes and apply it so every component shares a consistent look (and can switch light/dark). Inline colors everywhere, editing the library, or hard-coded per-component CSS are unmaintainable alternatives."
        },
        {
          "questionText": "Which tag can be used to display a card header in Angular Material?",
          "options": [
            {
              "text": "mat-card"
            },
            {
              "text": "mat-card-avatar"
            },
            {
              "text": "mat-card-header",
              "correct": true
            },
            {
              "text": "mat-card-image"
            }
          ],
          "explanation": "the mat-card-header is used to display the header information inside an Angular mat-card"
        },
        {
          "questionText": "Which tag can be used to display actions in Angular Material?",
          "options": [
            {
              "text": "mat-button"
            },
            {
              "text": "mat-raised-button"
            },
            {
              "text": "mat-card-content"
            },
            {
              "text": "mat-card-actions",
              "correct": true
            }
          ],
          "explanation": "the mat-card-actions tag is used to display actions inside an Angular Material template"
        },
        {
          "questionText": "Which part of Angular Material provides unstyled building blocks like drag-and-drop, overlays, and virtual scrolling?",
          "options": [
            {
              "text": "The Component Dev Kit (CDK).",
              "correct": true
            },
            {
              "text": "The Angular Router."
            },
            {
              "text": "HttpClient."
            },
            {
              "text": "The async pipe."
            }
          ],
          "explanation": "the Angular CDK (Component Dev Kit) provides reusable, unstyled behaviors — drag-and-drop, overlays, virtual scrolling, accessibility helpers — that Material builds on and that you can use directly. The Router handles navigation, HttpClient does HTTP, and the async pipe unwraps Observables."
        }
      ]
    },
    {
      "quizId": "forms",
      "milestone": "Angular Forms",
      "summary": "Angular forms build upon standard HTML forms to help create custom form controls and support easy validation.",
      "image": "https://raw.githubusercontent.com/marvinrusinek/codelab-angular-10-quiz-app/quiz/codelab-master/apps/playground/src/assets/images/forms.png",
      "difficulty": "intermediate",
      "facts": [
        "Angular has two form styles: template-driven (simple, ngModel-based) and reactive (an explicit form model in TypeScript).",
        "Reactive forms expose value and validity as observables, which makes complex or dynamic forms easier to manage and test.",
        "Angular ships built-in validators like required, minLength, email, and pattern, and lets you add custom validators."
      ],
      "questions": [
        {
          "questionText": "What is the first step to use Angular forms in a standalone component?",
          "options": [
            {
              "text": "Import FormsModule (template-driven) or ReactiveFormsModule (reactive) into the component's imports.",
              "correct": true
            },
            {
              "text": "Add a <form> tag and nothing else."
            },
            {
              "text": "Configure the form in angular.json."
            },
            {
              "text": "Nothing — form directives work without any import."
            }
          ],
          "explanation": "to use Angular forms in a standalone component you import FormsModule (for template-driven forms with ngModel) or ReactiveFormsModule (for reactive forms) into the component's imports array; without it the form directives are not recognized. The other options do not enable Angular's form features."
        },
        {
          "questionText": "What syntax provides two-way binding between an input and a component field in a template-driven form?",
          "options": [
            {
              "text": "[(ngModel)]",
              "correct": true
            },
            {
              "text": "{{ ngModel }}"
            },
            {
              "text": "[ngModel]"
            },
            {
              "text": "*ngModel"
            }
          ],
          "explanation": "the \"banana in a box\" syntax [(ngModel)]=\"field\" binds an input to a component field in both directions. {{ }} only reads a value, [ngModel] binds one way, and *ngModel is not valid syntax."
        },
        {
          "questionText": "In a reactive form, how do you define a form control in the component class?",
          "options": [
            {
              "text": "Create a FormControl, usually grouped in a FormGroup or built with FormBuilder.",
              "correct": true
            },
            {
              "text": "Only with [(ngModel)] in the template."
            },
            {
              "text": "By adding the required attribute in the HTML."
            },
            {
              "text": "With a plain string variable."
            }
          ],
          "explanation": "reactive forms define their model in the class with FormControl (typically grouped in a FormGroup, or built with FormBuilder), then bind it in the template with [formGroup] and formControlName. ngModel is for template-driven forms, required is a validator rather than a control, and a plain variable is not a form control."
        },
        {
          "questionText": "Which is a good reason to choose reactive forms over template-driven forms?",
          "options": [
            {
              "text": "The form model and validation live in the component class, which is explicit, easy to unit-test, and scales to complex, dynamic forms.",
              "correct": true
            },
            {
              "text": "Reactive forms remove the need for any validation."
            },
            {
              "text": "Template-driven forms cannot use validation at all."
            },
            {
              "text": "Reactive forms automatically submit the data to the server."
            }
          ],
          "explanation": "reactive forms keep the form model and validation in TypeScript, which is explicit, unit-testable, and well suited to complex or dynamic forms; template-driven forms are simpler for small cases. Both support validation, and neither submits to a server on its own."
        },
        {
          "questionText": "How do you provide validation for your inputs?",
          "options": [
            {
              "text": "Using required keyword",
              "correct": true
            },
            {
              "text": "Using valid keyword"
            },
            {
              "text": "Using matInput"
            },
            {
              "text": "Using mat-error"
            }
          ],
          "explanation": "by placing the \"required\" keyword on an input field, it marks the field for validation"
        },
        {
          "questionText": "Which of the following are steps to display a validation error?",
          "options": [
            {
              "text": "Use #name=\"ngModel\" binding.",
              "correct": true
            },
            {
              "text": "Use usernameModel's errors property.",
              "correct": true
            },
            {
              "text": "Use mat-error."
            },
            {
              "text": "Use required keyword."
            }
          ],
          "explanation": "using #name=\"ngModel\" and using usernameModel's error property are the two steps to display a validation error"
        },
        {
          "questionText": "Which is NOT a built-in validator that Angular provides?",
          "options": [
            {
              "text": "min"
            },
            {
              "text": "minLength"
            },
            {
              "text": "required"
            },
            {
              "text": "password",
              "correct": true
            }
          ],
          "explanation": "password is not a built-in validator which Angular provides"
        },
        {
          "questionText": "Which is true if the user changed the value of the input?",
          "options": [
            {
              "text": "dirty",
              "correct": true
            },
            {
              "text": "touched"
            },
            {
              "text": "untouched"
            },
            {
              "text": "pristine"
            }
          ],
          "explanation": "dirty is true when the user changes the value of the input "
        },
        {
          "questionText": "Which is true if the user focused on the input and then blurred without changing the value?",
          "options": [
            {
              "text": "dirty"
            },
            {
              "text": "touched",
              "correct": true
            },
            {
              "text": "untouched"
            },
            {
              "text": "pristine"
            }
          ],
          "explanation": "touched is true when the user focuses on input and then blurred"
        },
        {
          "questionText": "Which of the following wraps the input in an Angular Material form?",
          "options": [
            {
              "text": "mat-form-field",
              "correct": true
            },
            {
              "text": "matInput"
            },
            {
              "text": "mat-error"
            },
            {
              "text": "mat-card"
            }
          ],
          "explanation": "mat-form-field surrounds the input in an Angular Material form"
        },
        {
          "questionText": "How is an error shown in an Angular Material form?",
          "options": [
            {
              "text": "mat-form-field"
            },
            {
              "text": "matInput"
            },
            {
              "text": "mat-error",
              "correct": true
            },
            {
              "text": "mat-card"
            }
          ],
          "explanation": "the mat-error tag is used to show an error in an Angular Material form"
        }
      ]
    },
    {
      "quizId": "angular-cli",
      "milestone": "Angular-CLI",
      "summary": "The Angular CLI is a command-line interface tool used for initializing, developing, scaffolding and maintaining Angular applications.",
      "image": "https://raw.githubusercontent.com/marvinrusinek/codelab-angular-10-quiz-app/quiz/codelab-master/apps/playground/src/assets/images/angular-cli.png",
      "difficulty": "beginner",
      "facts": [
        "ng build applies production optimizations by default — tree shaking, minification, and bundling — using esbuild-based tooling.",
        "ng generate scaffolds standalone components, services, and directives following Angular's conventions automatically.",
        "ng update migrates your app across Angular versions, running automated code migrations (schematics) where possible."
      ],
      "questions": [
        {
          "questionText": "Which is a prerequisite for using Angular-CLI?",
          "options": [
            {
              "text": "NodeJS",
              "correct": true
            },
            {
              "text": "JavaScript"
            },
            {
              "text": "Github"
            },
            {
              "text": "WebStorm"
            }
          ],
          "explanation": "NodeJS needs to be installed on your machine before setting up Angular-CLI"
        },
        {
          "questionText": "How is Angular-CLI installed?",
          "options": [
            {
              "text": "npm install -g @angular/cli",
              "correct": true
            },
            {
              "text": "ng new \"my-app-name"
            },
            {
              "text": "cd my-app-name"
            },
            {
              "text": "ng serve command"
            }
          ],
          "explanation": "Angular-CLI is installed using the \"npm install -g @angular/cli\" command"
        },
        {
          "questionText": "How can you run an app that is built with Angular-CLI?",
          "options": [
            {
              "text": "ng serve",
              "correct": true
            },
            {
              "text": "ng new \"my-app-name\""
            },
            {
              "text": "npm start"
            },
            {
              "text": "node -v"
            }
          ],
          "explanation": "ng serve is the command that is required to run an app built with Angular-CLI"
        },
        {
          "questionText": "Which URL should be viewed in the browser to see the app running?",
          "options": [
            {
              "text": "http://www.google.com"
            },
            {
              "text": "http://angular.io"
            },
            {
              "text": "http://localhost"
            },
            {
              "text": "http://localhost:4200/",
              "correct": true
            }
          ],
          "explanation": "viewing http://localhost:4200/ allows us to view the app in the browser"
        },
        {
          "questionText": "What command is used to create a new service using the Angular-CLI?",
          "options": [
            {
              "text": "ng g c my-component"
            },
            {
              "text": "ng g m my-module"
            },
            {
              "text": "ng g s my-service",
              "correct": true
            },
            {
              "text": "ng g p my-pipe"
            }
          ],
          "explanation": "a service is created using the ng g s shortcut command"
        },
        {
          "questionText": "Which is a true statement about the Angular CLI?",
          "options": [
            {
              "text": "It scaffolds and generates code such as components and services."
            },
            {
              "text": "It runs a development server with live reload."
            },
            {
              "text": "It handles building and bundling the app for you."
            },
            {
              "text": "All of the above.",
              "correct": true
            }
          ],
          "explanation": "the Angular CLI scaffolds and generates code, runs a live-reload dev server (ng serve), and builds and bundles the app (ng build) — so all of the above are true."
        },
        {
          "questionText": "In Angular 22, what does `ng generate component my-comp` create by default?",
          "options": [
            {
              "text": "A standalone component.",
              "correct": true
            },
            {
              "text": "A component that must be declared in an NgModule."
            },
            {
              "text": "An empty TypeScript file only."
            },
            {
              "text": "A brand-new Angular application."
            }
          ],
          "explanation": "modern Angular generates standalone components by default, so the new component manages its own imports and needs no NgModule declaration. It scaffolds a full component (class, template, styles, and spec), not an empty file or a new app."
        },
        {
          "questionText": "Which command produces an optimized build for deployment?",
          "options": [
            {
              "text": "ng build",
              "correct": true
            },
            {
              "text": "ng serve"
            },
            {
              "text": "ng test"
            },
            {
              "text": "ng lint"
            }
          ],
          "explanation": "ng build compiles the app with production optimizations (tree shaking, minification) by default and outputs the files you deploy. ng serve runs the dev server, ng test runs unit tests, and ng lint checks code style."
        }
      ]
    },
    {
      "quizId": "change-detection",
      "milestone": "Change Detection",
      "summary": "Change detection keeps the view in sync with your data — learn how it runs, the OnPush strategy, and how to optimize it.",
      "image": "assets/images/change-detection.svg",
      "difficulty": "advanced",
      "facts": [
        "Change detection runs top-down through the component tree, so smaller components let Angular skip untouched branches faster.",
        "The async pipe marks its component for check when its observable emits, which is why it pairs well with OnPush.",
        "Angular DevTools includes a profiler that visualizes change-detection cycles, helping you spot components that check too often."
      ],
      "questions": [
        {
          "questionText": "What is the primary job of Angular's change detection?",
          "options": [
            {
              "text": "Keep the view (DOM) in sync with the component's data model.",
              "correct": true
            },
            {
              "text": "Compile TypeScript into JavaScript."
            },
            {
              "text": "Bundle the application for production."
            },
            {
              "text": "Manage HTTP requests."
            }
          ],
          "explanation": "Change detection keeps the rendered view (DOM) in sync with the component's data model whenever that data changes."
        },
        {
          "questionText": "Which library does Angular use by default to know WHEN to run change detection?",
          "options": [
            {
              "text": "Zone.js",
              "correct": true
            },
            {
              "text": "RxJS"
            },
            {
              "text": "Lodash"
            },
            {
              "text": "Immer"
            }
          ],
          "explanation": "Zone.js patches async APIs (DOM events, timers, XHR) so Angular knows when something may have changed and runs change detection."
        },
        {
          "questionText": "Which change detection strategy checks a component only when an @Input reference changes, an event fires inside it, or an async-pipe observable emits?",
          "options": [
            {
              "text": "OnPush",
              "correct": true
            },
            {
              "text": "Default"
            },
            {
              "text": "Detached"
            },
            {
              "text": "CheckAlways"
            }
          ],
          "explanation": "ChangeDetectionStrategy.OnPush limits checks to new @Input references, events originating in the component, and async-pipe emissions."
        },
        {
          "questionText": "With OnPush, why must you pass a NEW object or array reference instead of mutating the existing one?",
          "options": [
            {
              "text": "OnPush compares @Input values by reference, so an in-place mutation keeps the same reference and is missed.",
              "correct": true
            },
            {
              "text": "OnPush deep-clones every input on each cycle."
            },
            {
              "text": "OnPush disables all @Input bindings."
            },
            {
              "text": "OnPush only works with primitive inputs."
            }
          ],
          "explanation": "OnPush uses reference equality for @Input. Mutating an object or array in place keeps the same reference, so the change goes undetected — provide a new reference instead."
        },
        {
          "questionText": "Which ChangeDetectorRef method marks a component and its ancestors to be checked on the next cycle (often used with OnPush after an async update)?",
          "options": [
            {
              "text": "markForCheck()",
              "correct": true
            },
            {
              "text": "detectChanges()"
            },
            {
              "text": "detach()"
            },
            {
              "text": "reattach()"
            }
          ],
          "explanation": "markForCheck() flags the component and its path to the root as dirty so they are checked on the next change detection run."
        },
        {
          "questionText": "Which of the following trigger Angular's change detection by default? Select all that apply.",
          "options": [
            {
              "text": "A DOM event such as a click",
              "correct": true
            },
            {
              "text": "An HTTP response via HttpClient",
              "correct": true
            },
            {
              "text": "A setTimeout callback",
              "correct": true
            },
            {
              "text": "Code run inside NgZone.runOutsideAngular()"
            }
          ],
          "explanation": "Zone.js patches DOM events, XHR, and timers, so each triggers change detection. Code inside runOutsideAngular() intentionally escapes the zone and does NOT trigger it."
        },
        {
          "questionText": "In a zoneless Angular app (no Zone.js), what does Angular rely on to know when to update the view?",
          "options": [
            {
              "text": "Signals and explicit notifications (e.g. signal reads, async pipe, markForCheck)",
              "correct": true
            },
            {
              "text": "Polling the DOM every 16ms"
            },
            {
              "text": "Manual page reloads"
            },
            {
              "text": "Nothing — Zone.js is always required"
            }
          ],
          "explanation": "Zoneless Angular schedules change detection from signals and explicit notifications instead of relying on Zone.js to patch async APIs."
        },
        {
          "questionText": "Which are valid ways to optimize change detection performance?",
          "options": [
            {
              "text": "Use the OnPush change detection strategy."
            },
            {
              "text": "Use immutable data so inputs get new references."
            },
            {
              "text": "Use the async pipe or signals instead of manual subscriptions."
            },
            {
              "text": "All of the above.",
              "correct": true
            }
          ],
          "explanation": "OnPush, immutable data (new references), and the async pipe or signals all reduce unnecessary change detection work."
        }
      ]
    },
    {
      "quizId": "http",
      "milestone": "Angular HTTP",
      "summary": "Talk to servers the modern Angular way — provideHttpClient, typed requests, params and headers, error handling, functional interceptors, and cancellation.",
      "image": "assets/images/http.svg",
      "difficulty": "intermediate",
      "facts": [
        "By default HttpClient parses the response body as JSON — set responseType to 'text', 'blob', or 'arraybuffer' when you need something else.",
        "HttpParams and HttpHeaders are immutable — methods like set() and append() return a new instance instead of mutating the original.",
        "The HttpClient testing tools (provideHttpClientTesting and HttpTestingController) let you unit-test HTTP calls without making real network requests."
      ],
      "questions": [
        {
          "questionText": "In a standalone Angular 22 app, how do you make HttpClient available for injection?",
          "options": [
            {
              "text": "Add provideHttpClient() to the providers passed to bootstrapApplication().",
              "correct": true
            },
            {
              "text": "Import HttpClientModule into every standalone component."
            },
            {
              "text": "Create it yourself with new HttpClient() wherever it is needed."
            },
            {
              "text": "Assign HttpClient to a global window variable at startup."
            }
          ],
          "explanation": "provideHttpClient() is added to the app's providers (in bootstrapApplication or appConfig) to make HttpClient injectable. HttpClientModule is the older, discouraged setup, and you never construct HttpClient yourself."
        },
        {
          "questionText": "Which snippet correctly obtains HttpClient inside a standalone service?",
          "options": [
            {
              "text": "private http = inject(HttpClient);",
              "correct": true
            },
            {
              "text": "private http = new HttpClient();"
            },
            {
              "text": "private http = HttpClient.get();"
            },
            {
              "text": "private http = @Inject();"
            }
          ],
          "explanation": "HttpClient is obtained through Angular's DI — most concisely with inject(HttpClient), or via constructor injection. You never create it with new, and HttpClient.get() is not a static factory."
        },
        {
          "questionText": "Which of the following statements about HTTP method semantics are correct? Select all that apply.",
          "options": [
            {
              "text": "GET retrieves data and should not change server state.",
              "correct": true
            },
            {
              "text": "POST is typically used to create a new resource.",
              "correct": true
            },
            {
              "text": "PUT replaces a resource, while PATCH applies a partial update.",
              "correct": true
            },
            {
              "text": "DELETE is used to fetch a resource without removing it."
            }
          ],
          "explanation": "GET is safe and read-only, POST creates resources, and PUT replaces a resource while PATCH updates it partially. DELETE removes a resource — it does not fetch one."
        },
        {
          "questionText": "How do you tell HttpClient to type the response body as a User object?",
          "options": [
            {
              "text": "this.http.get<User>('/api/users/1')",
              "correct": true
            },
            {
              "text": "this.http.get('/api/users/1', User)"
            },
            {
              "text": "this.http.get('/api/users/1', { type: User })"
            },
            {
              "text": "this.http.getTyped(User, '/api/users/1')"
            }
          ],
          "explanation": "HttpClient methods take a generic type, so http.get<User>(url) returns an Observable<User> with a typed body. It is a compile-time generic — there is no runtime type argument or getTyped() method."
        },
        {
          "questionText": "Which is the correct way to add the query parameter ?page=2 to a GET request?",
          "options": [
            {
              "text": "this.http.get('/api/items', { params: new HttpParams().set('page', 2) })",
              "correct": true
            },
            {
              "text": "this.http.get('/api/items?params=page:2')"
            },
            {
              "text": "this.http.get('/api/items', { query: { page: 2 } })"
            },
            {
              "text": "this.http.get('/api/items').setParam('page', 2)"
            }
          ],
          "explanation": "query parameters go in the params option, e.g. { params: new HttpParams().set('page', 2) }, which Angular serializes to ?page=2. There is no query option or setParam() method."
        },
        {
          "questionText": "How do you attach an Authorization header to a single POST request?",
          "options": [
            {
              "text": "this.http.post('/api/items', body, { headers: new HttpHeaders().set('Authorization', token) })",
              "correct": true
            },
            {
              "text": "this.http.post('/api/items', body, { Authorization: token })"
            },
            {
              "text": "this.http.post('/api/items', body).header('Authorization', token)"
            },
            {
              "text": "this.http.setHeaders({ Authorization: token }).post('/api/items', body)"
            }
          ],
          "explanation": "per-request headers go in the headers option using HttpHeaders, e.g. { headers: new HttpHeaders().set('Authorization', token) }. For a header on every request, a functional interceptor is the better choice."
        },
        {
          "questionText": "Which statements about handling HttpClient errors with catchError() are correct? Select all that apply.",
          "options": [
            {
              "text": "catchError() is placed inside the observable's pipe() to intercept a failed request.",
              "correct": true
            },
            {
              "text": "Inside catchError() you can return a fallback observable such as of([]) to recover gracefully.",
              "correct": true
            },
            {
              "text": "Re-throwing with throwError(() => err) lets the error propagate to the subscriber.",
              "correct": true
            },
            {
              "text": "catchError() automatically retries the failed request three times."
            }
          ],
          "explanation": "catchError() is an RxJS operator used inside pipe() to handle a failed request. You can recover by returning a fallback such as of([]) or re-throw with throwError(() => err); it does not retry on its own."
        },
        {
          "questionText": "In Angular 22, functional HTTP interceptors are plain functions registered with withInterceptors() (passed to provideHttpClient()) that can modify both outgoing requests and incoming responses. True or False?",
          "options": [
            {
              "text": "True",
              "correct": true
            },
            {
              "text": "False"
            }
          ],
          "explanation": "functional interceptors are plain functions — (req, next) => next(req) — registered with withInterceptors() in provideHttpClient(). They transform the outgoing request and incoming response, replacing the class-based HTTP_INTERCEPTORS."
        },
        {
          "questionText": "A search box fires a GET request on each keystroke. Which approach best cancels the previous in-flight request when a new keystroke arrives?",
          "options": [
            {
              "text": "Pipe the keystrokes through switchMap() into the HttpClient call, so a new emission unsubscribes from the previous request and Angular aborts it if it is still in flight.",
              "correct": true
            },
            {
              "text": "Call request.cancel() on the object returned by http.get()."
            },
            {
              "text": "Pass { cancelPrevious: true } in the request options."
            },
            {
              "text": "Nothing can be done — an HTTP request can always be cancelled retroactively once it has been sent."
            }
          ],
          "explanation": "switchMap() unsubscribes from the previous request when a new value arrives, aborting it only if it is still in flight. There is no request.cancel() or cancelPrevious option, and an already-completed request cannot be undone."
        },
        {
          "questionText": "Which are recommended best practices for using HttpClient in Angular 22? Select all that apply.",
          "options": [
            {
              "text": "Encapsulate HTTP calls in dedicated services instead of calling HttpClient from components.",
              "correct": true
            },
            {
              "text": "Use typed responses and handle failures with catchError().",
              "correct": true
            },
            {
              "text": "Use functional interceptors for cross-cutting concerns like auth headers and logging.",
              "correct": true
            },
            {
              "text": "Subscribe to the same request observable in many places to reuse a single network call."
            }
          ],
          "explanation": "wrap HTTP calls in services, use typed responses with catchError(), and put cross-cutting concerns in interceptors. Subscribing to the same observable in many places does not reuse one call (they are cold) — use shareReplay() or the async pipe."
        }
      ]
    },
    {
      "quizId": "testing",
      "milestone": "Angular Testing",
      "summary": "Test Angular the modern way — TestBed and fixtures, change detection, injected services, spies and mocks, async, signals, HTTP, and choosing the right test level.",
      "image": "assets/images/testing.svg",
      "difficulty": "intermediate",
      "facts": [
        "Playwright tests run through a real browser context, while Angular Jest unit tests run in a faster, more isolated jsdom environment.",
        "Excessive mocking can make a test pass even when the real, integrated behavior is broken — so mock only what a test genuinely needs to isolate.",
        "TestBed builds a dedicated testing module for each test, keeping tests isolated from the real application bootstrap and from each other."
      ],
      "questions": [
        {
          "questionText": "You want to verify a single pure function that formats a score, in isolation and as fast as possible. Which type of test is the best fit?",
          "options": [
            {
              "text": "An end-to-end (Playwright) test that clicks through the UI."
            },
            {
              "text": "A unit test.",
              "correct": true
            },
            {
              "text": "A manual test in the browser each time."
            },
            {
              "text": "An integration test spanning several components and services."
            }
          ],
          "explanation": "a unit test verifies a small, isolated piece of logic like a pure function — fast and without spinning up the whole app. End-to-end tests exercise real user flows and are slower; pick the lowest level that gives useful confidence."
        },
        {
          "questionText": "What is the role of TestBed.configureTestingModule({ ... }) in an Angular test?",
          "options": [
            {
              "text": "It sets up the testing module — the standalone imports, declarations, and providers the test needs.",
              "correct": true
            },
            {
              "text": "It launches a real browser to run the test."
            },
            {
              "text": "It compiles the production bundle."
            },
            {
              "text": "It replaces Jest's expect() assertions."
            }
          ],
          "explanation": "TestBed.configureTestingModule() sets up the testing module — the imports, declarations, and providers a test needs — before anything is created. It does not launch a browser, build for production, or provide assertions (that is Jest's expect())."
        },
        {
          "questionText": "After configuring TestBed, how do you create a component and get access to it for assertions?",
          "options": [
            {
              "text": "const comp = new MyComponent();"
            },
            {
              "text": "const comp = TestBed.inject(MyComponent);"
            },
            {
              "text": "const fixture = TestBed.createComponent(MyComponent), then read fixture.componentInstance.",
              "correct": true
            },
            {
              "text": "const fixture = MyComponent.render();"
            }
          ],
          "explanation": "TestBed.createComponent(MyComponent) returns a ComponentFixture: fixture.componentInstance is the component and fixture.nativeElement is its rendered DOM. Using new bypasses DI and rendering, and TestBed.inject is for services."
        },
        {
          "questionText": "A test updates a component's data, then asserts the rendered text in fixture.nativeElement — but the DOM still shows the old value. What is the most likely fix?",
          "options": [
            {
              "text": "Construct the component again with new."
            },
            {
              "text": "Call fixture.detectChanges() to run change detection before asserting the DOM.",
              "correct": true
            },
            {
              "text": "Restart the Jest process."
            },
            {
              "text": "Read from the production DOM instead of nativeElement."
            }
          ],
          "explanation": "Angular does not auto-render on every change in a test, so call fixture.detectChanges() to run change detection and update the DOM before asserting on fixture.nativeElement. Creating a fixture does not guarantee every cycle runs for you."
        },
        {
          "questionText": "You are testing a ScoreService that depends on other providers. What is the recommended way to obtain it in a Jest + TestBed test?",
          "options": [
            {
              "text": "const svc = new ScoreService(); with no providers configured."
            },
            {
              "text": "Import the instance from the component under test."
            },
            {
              "text": "Read it off the global window object."
            },
            {
              "text": "Register providers in TestBed, then const svc = TestBed.inject(ScoreService);",
              "correct": true
            }
          ],
          "explanation": "when a service's dependencies or providers matter, get it through DI: register providers in TestBed and retrieve it with TestBed.inject(ScoreService). Using new bypasses DI — direct construction only fits a dependency-free class."
        },
        {
          "questionText": "Which statements about test doubles in this Jest-based project are correct? Select all that apply.",
          "options": [
            {
              "text": "jest.fn() creates a mock function whose calls and arguments you can assert.",
              "correct": true
            },
            {
              "text": "jest.spyOn(obj, 'method') wraps an existing method so you can observe or override it.",
              "correct": true
            },
            {
              "text": "A stub supplies canned return values to keep a test focused and deterministic.",
              "correct": true
            },
            {
              "text": "jasmine.createSpyObj() is the standard way to create spies in this project."
            }
          ],
          "explanation": "this project uses Jest, so jest.fn() creates assertable mock functions, jest.spyOn() wraps existing methods, and stubs return canned values for determinism. jasmine.createSpyObj() belongs to Jasmine, which this project does not use."
        },
        {
          "questionText": "Which are valid ways to test asynchronous code in an Angular + Jest test? Select all that apply.",
          "options": [
            {
              "text": "Use async/await with a promise-returning API.",
              "correct": true
            },
            {
              "text": "Use fakeAsync() with tick() to advance virtual time for timers.",
              "correct": true
            },
            {
              "text": "Assume the assertion runs after the async work with no awaiting or flushing."
            },
            {
              "text": "Await fixture.whenStable() to let pending async work settle before asserting.",
              "correct": true
            }
          ],
          "explanation": "async/await handles promise-based work, fakeAsync() with tick() advances virtual time for timers, and fixture.whenStable() waits for pending async tasks before you assert. Asserting with no awaiting or flushing checks state before the work finishes."
        },
        {
          "questionText": "Which are good practices when testing a component that uses Angular Signals? Select all that apply.",
          "options": [
            {
              "text": "Reach into and directly overwrite the component's private signals from the test."
            },
            {
              "text": "Trigger the state change the test needs, then read the resulting signal or computed value.",
              "correct": true
            },
            {
              "text": "Call fixture.detectChanges() before asserting on the rendered DOM.",
              "correct": true
            },
            {
              "text": "Assert the externally observable state or output rather than a signal's internal details.",
              "correct": true
            }
          ],
          "explanation": "test Signals by driving the required state change and reading the resulting value or rendered output, running change detection before DOM assertions. Focus on observable behavior — mutating private signals couples the test to implementation details."
        },
        {
          "questionText": "Using Angular's HTTP testing utilities with HttpTestingController, what is the correct order to test a GET request?",
          "options": [
            {
              "text": "Flush a mock response first, then make the request."
            },
            {
              "text": "Allow a real network request, then assert the parsed body."
            },
            {
              "text": "Make the request, match it with expectOne() and assert it, then flush() the mock response and verify().",
              "correct": true
            },
            {
              "text": "Call fixture.detectChanges() to send the HTTP request."
            }
          ],
          "explanation": "with HttpTestingController you trigger the request, match it with expectOne() (asserting method and URL), then flush() a mock response and call verify(). Unit tests must never hit the real network, and you assert the request before flushing."
        },
        {
          "questionText": "True or False: Playwright end-to-end tests are the best choice for verifying every small service method and individual component in isolation.",
          "options": [
            {
              "text": "True"
            },
            {
              "text": "False",
              "correct": true
            }
          ],
          "explanation": "Playwright runs real-browser end-to-end tests — ideal for critical user flows, routing, and cross-page behavior, but slower than unit tests. Small, isolated logic belongs in fast Jest unit tests; choose the lowest practical test level that gives useful confidence."
        }
      ]
    },
    {
      "quizId": "performance",
      "milestone": "Angular Performance",
      "summary": "Build faster Angular apps — change detection and OnPush, signals, @for track, lazy loading, @defer, image and template optimization, and smaller bundles.",
      "image": "assets/images/performance.svg",
      "difficulty": "intermediate",
      "facts": [
        "Hydration lets a server-rendered Angular app become interactive without re-rendering the whole page, reducing flicker and work on load.",
        "Zoneless change detection (dropping Zone.js) lets Angular schedule updates from signals, cutting the overhead of patched async APIs.",
        "Standalone components with the modern esbuild-based builder produce smaller, faster builds than legacy NgModule setups."
      ],
      "questions": [
        {
          "questionText": "Why does front-end performance matter for an Angular application?",
          "options": [
            {
              "text": "It only matters for the development build, not production."
            },
            {
              "text": "It affects TypeScript compile time but not the end user."
            },
            {
              "text": "Faster load and response times improve user experience, engagement, and conversion.",
              "correct": true
            },
            {
              "text": "It changes the app's business logic and features."
            }
          ],
          "explanation": "performance directly shapes the user experience — faster startup and interaction keep users engaged and improve conversion. It is a runtime, user-facing concern, and optimizing it does not change the app's features."
        },
        {
          "questionText": "How does the OnPush change detection strategy help performance?",
          "options": [
            {
              "text": "It disables change detection entirely for the whole app."
            },
            {
              "text": "It skips checking a component unless an @Input reference changes, an event fires in it, or an async pipe emits.",
              "correct": true
            },
            {
              "text": "It guarantees every app becomes faster with no trade-offs."
            },
            {
              "text": "It re-renders the component on every browser animation frame."
            }
          ],
          "explanation": "OnPush limits a component's checks to new @Input references, events inside it, and async-pipe emissions, so Angular skips large parts of the tree. It is not a guaranteed win — it relies on immutable data, so mutating an object in place can cause a missed update."
        },
        {
          "questionText": "Which statements about Angular Signals and performance are correct? Select all that apply.",
          "options": [
            {
              "text": "Signals replace RxJS for all asynchronous streams and HTTP."
            },
            {
              "text": "A signal notifies only the consumers that read it, enabling fine-grained updates.",
              "correct": true
            },
            {
              "text": "computed() derives a value that recalculates only when its signal dependencies change.",
              "correct": true
            },
            {
              "text": "Signals make change detection more targeted than checking whole component trees.",
              "correct": true
            }
          ],
          "explanation": "a signal notifies only the consumers that read it, and computed() recalculates lazily when its dependencies change — far more targeted than checking whole trees. But signals model synchronous state and do not replace RxJS for streams like HTTP."
        },
        {
          "questionText": "In a large @for list that updates often, what does a track expression (e.g. @for (item of items; track item.id)) do for performance?",
          "options": [
            {
              "text": "It lets Angular match items by identity and reuse existing DOM instead of recreating every row.",
              "correct": true
            },
            {
              "text": "It sorts the list automatically on each change."
            },
            {
              "text": "It caches the list in localStorage."
            },
            {
              "text": "It disables change detection for the list."
            }
          ],
          "explanation": "a track expression gives each item a stable identity, so Angular re-uses and moves existing DOM nodes instead of recreating every row when the array changes. Without it, a new array reference can force Angular to rebuild the whole list."
        },
        {
          "questionText": "What is the main performance benefit of lazy loading a feature route with loadComponent or loadChildren?",
          "options": [
            {
              "text": "It makes every subsequent navigation instant."
            },
            {
              "text": "It removes the need for a production build."
            },
            {
              "text": "It eliminates change detection for that feature."
            },
            {
              "text": "It keeps that feature's code out of the initial bundle, so the app starts faster.",
              "correct": true
            }
          ],
          "explanation": "lazy loading splits a feature's code into a separate chunk that downloads on demand, shrinking the initial bundle and speeding up startup. It does not make later navigations instant — the chunk still downloads the first time it is needed."
        },
        {
          "questionText": "What does wrapping a heavy component in @defer (on viewport) { ... } do?",
          "options": [
            {
              "text": "It renders the component immediately but hides it with CSS."
            },
            {
              "text": "It delays loading and rendering that component until it enters the viewport, improving initial render.",
              "correct": true
            },
            {
              "text": "It runs the component on a web worker."
            },
            {
              "text": "It permanently disables the component's change detection."
            }
          ],
          "explanation": "@defer postpones loading and rendering its content until a trigger fires (here, entering the viewport), so heavy or below-the-fold content does not block the first paint and its code can be split out. It defers real work, not just hides it."
        },
        {
          "questionText": "How do you optimize images in Angular 22 using the built-in directive?",
          "options": [
            {
              "text": "Add loading='eager' to every img so they all load up front."
            },
            {
              "text": "Inline every image as a base64 data URL in the template."
            },
            {
              "text": "Use NgOptimizedImage with the ngSrc attribute to enable lazy loading, sizing, and priority hints.",
              "correct": true
            },
            {
              "text": "Replace all images with CSS background-image for speed."
            }
          ],
          "explanation": "NgOptimizedImage (via the ngSrc attribute) adds lazy loading, prevents layout shift by requiring width and height, and supports a priority hint for the LCP image. Loading every image eagerly or inlining base64 URLs bloats the initial payload."
        },
        {
          "questionText": "Which practices help avoid unnecessary template work during change detection? Select all that apply.",
          "options": [
            {
              "text": "Move expensive calculations out of the template into a computed() or a memoized value.",
              "correct": true
            },
            {
              "text": "Put complex getters in bindings so they recompute on every change detection cycle."
            },
            {
              "text": "Avoid calling functions that do heavy work directly in template bindings.",
              "correct": true
            },
            {
              "text": "Use the async pipe or signals so values update only when the data changes.",
              "correct": true
            }
          ],
          "explanation": "template expressions run on every change detection cycle, so heavy functions or getters in bindings repeat that work constantly. Precompute with computed(), keep heavy functions out of bindings, and use the async pipe or signals so work happens only when the data changes."
        },
        {
          "questionText": "Which of these reduce an Angular app's bundle size? Select all that apply.",
          "options": [
            {
              "text": "Building with the production configuration so tree shaking and minification run.",
              "correct": true
            },
            {
              "text": "Code splitting via lazy-loaded routes and @defer.",
              "correct": true
            },
            {
              "text": "Removing unused code and dependencies (dead-code elimination).",
              "correct": true
            },
            {
              "text": "Importing a whole utility library just to use a single function."
            }
          ],
          "explanation": "production builds enable tree shaking and minification, lazy routes and @defer split code into on-demand chunks, and removing unused code trims the payload. Importing a whole library for one function does the opposite, pulling in dead weight."
        },
        {
          "questionText": "True or False: You should measure and profile an app's performance to find the real bottlenecks before optimizing, rather than guessing.",
          "options": [
            {
              "text": "True",
              "correct": true
            },
            {
              "text": "False"
            }
          ],
          "explanation": "measuring with the browser profiler, Lighthouse, or Angular DevTools shows where time is actually spent, so you fix real bottlenecks instead of guessing. Premature optimization from assumptions adds complexity without improving the metrics that matter."
        }
      ]
    },
    {
      "quizId": "rxjs",
      "milestone": "RxJS",
      "summary": "Think reactively in Angular — Observables and operators, subjects, combining streams, error handling, and preventing memory leaks with the async pipe and takeUntil.",
      "image": "assets/images/rxjs.svg",
      "difficulty": "advanced",
      "facts": [
        "Cold Observables (like HttpClient calls) start a fresh execution for each subscriber, while hot Observables (like a Subject) share one execution among all subscribers.",
        "shareReplay() lets multiple subscribers share a single underlying execution and replays the last result, avoiding duplicate HTTP requests.",
        "RxJS operators are pure, pipeable functions that return a new Observable, so unused operators are tree-shaken out of the production bundle."
      ],
      "questions": [
        {
          "questionText": "What best describes an Observable in RxJS?",
          "options": [
            {
              "text": "A value that is computed once and stored synchronously."
            },
            {
              "text": "A lazy stream of values over time that does nothing until you subscribe.",
              "correct": true
            },
            {
              "text": "A JavaScript Promise with a few extra methods."
            },
            {
              "text": "A global event bus shared across the whole app."
            }
          ],
          "explanation": "an Observable is a lazy stream of values over time — it does nothing until you subscribe, then emits zero or more values, an error, or completion. Unlike a Promise (eager, single value, not cancellable), it is lazy, can emit many values, and is cancellable; it is not a stored value or a global event bus."
        },
        {
          "questionText": "You have an array [1, 2, 3]. Which creation function emits each element as a separate value?",
          "options": [
            {
              "text": "of([1, 2, 3])"
            },
            {
              "text": "interval([1, 2, 3])"
            },
            {
              "text": "from([1, 2, 3])",
              "correct": true
            },
            {
              "text": "timer([1, 2, 3])"
            }
          ],
          "explanation": "from() turns an array (or Promise/iterable) into an Observable that emits each element in turn — 1, then 2, then 3. of([1,2,3]) emits the whole array as one value, while interval and timer produce time-based emissions and do not take an array of values."
        },
        {
          "questionText": "Which pipe keeps only the even numbers and then doubles them?",
          "options": [
            {
              "text": "pipe(filter(n => n % 2 === 0), map(n => n * 2))",
              "correct": true
            },
            {
              "text": "pipe(map(n => n * 2), filter(n => n % 2 === 0))"
            },
            {
              "text": "pipe(tap(n => n % 2 === 0), map(n => n * 2))"
            },
            {
              "text": "pipe(switchMap(n => n * 2))"
            }
          ],
          "explanation": "operators in pipe() run in order, so filter drops the odd numbers first and map then doubles the survivors. Option 2 doubles first, making every value even, so the filter removes nothing; tap only runs side effects and cannot filter; and switchMap must return an Observable, not a number."
        },
        {
          "questionText": "What is the correct use of the tap() operator?",
          "options": [
            {
              "text": "To transform each emitted value into a new value."
            },
            {
              "text": "To filter out unwanted values."
            },
            {
              "text": "To flatten an inner Observable into the outer stream."
            },
            {
              "text": "To perform a side effect (like logging) without changing the emitted values.",
              "correct": true
            }
          ],
          "explanation": "tap() runs a side effect — logging, debugging, updating something external — for each emission while passing the value through unchanged. Transforming values is map(), removing values is filter(), and flattening an inner Observable is a higher-order operator such as switchMap or mergeMap."
        },
        {
          "questionText": "Which statements about RxJS Subjects are correct? Select all that apply.",
          "options": [
            {
              "text": "A Subject is both an Observable and an Observer, so it can multicast values to many subscribers.",
              "correct": true
            },
            {
              "text": "A plain Subject replays its last value to any subscriber that arrives late."
            },
            {
              "text": "A BehaviorSubject requires an initial value and gives new subscribers the current value immediately.",
              "correct": true
            },
            {
              "text": "A ReplaySubject can re-emit a buffer of previous values to new subscribers.",
              "correct": true
            }
          ],
          "explanation": "a Subject is both an Observable and an Observer, so it multicasts to many subscribers; a BehaviorSubject holds a current value (needing an initial one) and hands it to new subscribers immediately; and a ReplaySubject buffers and re-emits past values. A plain Subject does NOT replay — a late subscriber only receives values emitted after it subscribes."
        },
        {
          "questionText": "Why is the async pipe (data$ | async) usually preferred over manually subscribing in a component?",
          "options": [
            {
              "text": "It makes the Observable emit its values faster."
            },
            {
              "text": "It subscribes for you and automatically unsubscribes when the component is destroyed, preventing leaks.",
              "correct": true
            },
            {
              "text": "It converts the Observable into a Promise."
            },
            {
              "text": "It caches the last value in localStorage."
            }
          ],
          "explanation": "the async pipe subscribes to the Observable, renders each emitted value, and automatically unsubscribes when the component is destroyed — removing a common memory-leak source and a lot of boilerplate. It does not speed up emissions, turn the Observable into a Promise, or persist values to localStorage."
        },
        {
          "questionText": "A type-ahead search fires an HTTP request on each keystroke. Which operator cancels the previous in-flight request when a new keystroke arrives?",
          "options": [
            {
              "text": "mergeMap"
            },
            {
              "text": "concatMap"
            },
            {
              "text": "switchMap",
              "correct": true
            },
            {
              "text": "exhaustMap"
            }
          ],
          "explanation": "switchMap unsubscribes from the previous inner Observable when a new value arrives, cancelling the outdated request — ideal for type-ahead search. mergeMap runs every request concurrently with no cancellation, concatMap queues them in order, and exhaustMap ignores new input until the current request finishes."
        },
        {
          "questionText": "Which statements about combining Observables are correct? Select all that apply.",
          "options": [
            {
              "text": "forkJoin waits for all source Observables to complete, then emits their last values once.",
              "correct": true
            },
            {
              "text": "combineLatest emits an array of the latest values whenever any source emits.",
              "correct": true
            },
            {
              "text": "zip pairs values by index, emitting only when every source has produced the next item.",
              "correct": true
            },
            {
              "text": "forkJoin emits a new combined value every time any single source emits."
            }
          ],
          "explanation": "forkJoin is like Promise.all — it waits for every source to complete and emits their final values once, great for firing several HTTP requests in parallel; combineLatest emits the latest values whenever any source emits; and zip pairs values by index. forkJoin does NOT emit continuously — that behavior belongs to combineLatest."
        },
        {
          "questionText": "Which are correct ways to handle errors and prevent memory leaks with RxJS in Angular? Select all that apply.",
          "options": [
            {
              "text": "Rely on Angular to automatically unsubscribe from every manual .subscribe() call."
            },
            {
              "text": "Use catchError to intercept an error and return a fallback Observable such as of([]).",
              "correct": true
            },
            {
              "text": "Use takeUntil(this.destroy$) so subscriptions complete when the component is destroyed.",
              "correct": true
            },
            {
              "text": "Prefer the async pipe so subscription and cleanup are handled for you.",
              "correct": true
            }
          ],
          "explanation": "catchError intercepts a failed stream and lets you recover by returning a fallback Observable; takeUntil(destroy$) completes subscriptions when the component is destroyed; and the async pipe subscribes and unsubscribes automatically. Angular does NOT auto-unsubscribe from manual .subscribe() calls — leaving them open is a common memory-leak source."
        },
        {
          "questionText": "True or False: Nesting .subscribe() calls inside one another is the recommended way to chain dependent Observable requests in Angular.",
          "options": [
            {
              "text": "True"
            },
            {
              "text": "False",
              "correct": true
            }
          ],
          "explanation": "nested subscriptions are hard to read, error-prone, and leak easily. The reactive approach chains dependent requests with a higher-order operator like switchMap (or concatMap), keeping a single subscription; that also cancels stale inner requests and lets the async pipe manage cleanup."
        }
      ]
    },
    {
      "quizId": "signals",
      "milestone": "Angular Signals",
      "summary": "Master reactive state in Angular — writable signals, computed derived state, effects, signals in templates, and when to reach for signals instead of RxJS.",
      "image": "assets/images/signals.svg",
      "difficulty": "advanced",
      "facts": [
        "input() creates a signal-based component input that replaces the @Input decorator and is read like any other signal in templates and computed values.",
        "toSignal() and toObservable() bridge RxJS and Signals, so you can adopt Signals incrementally alongside existing Observable code.",
        "In a zoneless Angular app, signals are the primary way Angular knows when to update the view, replacing Zone.js-based change detection."
      ],
      "questions": [
        {
          "questionText": "What is an Angular Signal?",
          "options": [
            {
              "text": "A one-time value that never changes after it is created."
            },
            {
              "text": "An RxJS Observable with a different name."
            },
            {
              "text": "A reactive wrapper around a value that notifies its consumers when the value changes.",
              "correct": true
            },
            {
              "text": "A directive that updates the DOM directly."
            }
          ],
          "explanation": "a signal is a reactive wrapper around a value: you read it by calling it like a function, and when its value changes it notifies everything that depends on it — computed signals, templates, and effects. It is not a fixed constant, it is not an Observable (signals are synchronous and always hold a current value), and it is not a directive."
        },
        {
          "questionText": "How do you create a writable signal holding the number 0, and then read its value?",
          "options": [
            {
              "text": "const count = signal(0); then read it with count().",
              "correct": true
            },
            {
              "text": "const count = signal(0); then read it with count.value."
            },
            {
              "text": "const count = new Signal(0); then read it with count.get()."
            },
            {
              "text": "const count = computed(() => 0); then read it with count()."
            }
          ],
          "explanation": "signal(0) creates a writable signal with an initial value, and you read it by calling it as a function: count(). Signals are not read with .value or .get(), you do not construct them with new, and computed() creates a read-only derived signal rather than a writable one."
        },
        {
          "questionText": "You have count = signal(0) and want to increment it based on its current value. Which is the idiomatic way?",
          "options": [
            {
              "text": "count.set(count + 1)"
            },
            {
              "text": "count.update(n => n + 1)",
              "correct": true
            },
            {
              "text": "count = count() + 1"
            },
            {
              "text": "count.value++"
            }
          ],
          "explanation": "update() receives the current value and returns the new one, so count.update(n => n + 1) is the idiomatic way to derive the next value from the old. set() replaces the value outright and fits when the new value does not depend on the old (e.g. count.set(5)); reassigning the variable or using .value is not how signals work."
        },
        {
          "questionText": "You have firstName and lastName signals. How do you create a fullName that stays in sync automatically?",
          "options": [
            {
              "text": "const fullName = signal(firstName() + ' ' + lastName());"
            },
            {
              "text": "const fullName = effect(() => firstName() + ' ' + lastName());"
            },
            {
              "text": "const fullName = firstName() + ' ' + lastName();"
            },
            {
              "text": "const fullName = computed(() => firstName() + ' ' + lastName());",
              "correct": true
            }
          ],
          "explanation": "computed() creates a read-only signal that re-derives its value whenever any signal it reads changes, so fullName updates automatically. A plain signal(...) captures the value once and won't track changes, a bare expression is a one-time string, and effect() runs side effects and does not return a usable value."
        },
        {
          "questionText": "Which statements about computed() signals are correct? Select all that apply.",
          "options": [
            {
              "text": "A computed signal automatically tracks the signals it reads and recomputes when they change.",
              "correct": true
            },
            {
              "text": "A computed signal recalculates on every change detection cycle, even if its dependencies did not change."
            },
            {
              "text": "A computed signal is read-only; you cannot call set() or update() on it.",
              "correct": true
            },
            {
              "text": "A computed signal is lazy and memoized — it recomputes only when it is read after a dependency changed.",
              "correct": true
            }
          ],
          "explanation": "a computed signal tracks whichever signals it reads and recomputes when they change; it is read-only (no set/update); and it is lazy and memoized, recomputing only when read after a dependency changed. It does NOT recompute on every change detection cycle — that memoization is exactly what avoids wasted work."
        },
        {
          "questionText": "What is the primary purpose of effect()?",
          "options": [
            {
              "text": "To derive a new value from other signals."
            },
            {
              "text": "To run a side effect (like logging or syncing to localStorage) whenever the signals it reads change.",
              "correct": true
            },
            {
              "text": "To create a new writable signal."
            },
            {
              "text": "To manually trigger Angular change detection."
            }
          ],
          "explanation": "effect() runs a side effect — logging, syncing to storage, DOM work outside bindings — automatically whenever any signal it reads changes. Deriving a value is computed()'s job (prefer it over an effect that just sets another signal), creating state is signal(), and effects are not how you trigger change detection."
        },
        {
          "questionText": "How do you display a signal's value in a template, and what does Angular do when it changes?",
          "options": [
            {
              "text": "{{ count.value }} — Angular polls the signal on a timer."
            },
            {
              "text": "{{ count | async }} — the async pipe subscribes to the signal."
            },
            {
              "text": "{{ count() }} — Angular tracks the read and updates just that view when the signal changes.",
              "correct": true
            },
            {
              "text": "{{ count }} — Angular renders the signal object itself."
            }
          ],
          "explanation": "in the template you call the signal like a function, {{ count() }}, and Angular records that this view depends on it, so when it changes only the affected part of the view updates — no async pipe or manual subscription needed. Signals are not read with .value, the async pipe is for Observables, and rendering the bare signal would show the function, not its value."
        },
        {
          "questionText": "Which statements comparing Signals and RxJS are correct? Select all that apply.",
          "options": [
            {
              "text": "A signal always holds a current value you can read synchronously, similar to a BehaviorSubject's current value.",
              "correct": true
            },
            {
              "text": "Signals suit synchronous UI state, while RxJS excels at async streams and event pipelines.",
              "correct": true
            },
            {
              "text": "Reading a signal needs no subscribe/unsubscribe, avoiding that class of memory leak.",
              "correct": true
            },
            {
              "text": "Signals fully replace RxJS for HTTP requests and complex async coordination."
            }
          ],
          "explanation": "a signal always has a current value you can read synchronously (like a BehaviorSubject), it fits synchronous UI state while RxJS shines for async streams, and reading it needs no subscription so there is no unsubscribe leak. Signals do NOT replace RxJS for HTTP or complex async work — those still belong to Observables, often bridged with toSignal()."
        },
        {
          "questionText": "Which are recommended practices when working with Angular Signals? Select all that apply.",
          "options": [
            {
              "text": "Put heavy work and side effects inside computed() so the UI stays in sync."
            },
            {
              "text": "Keep computed() functions pure — no side effects and no mutating other signals.",
              "correct": true
            },
            {
              "text": "Prefer computed() over an effect() when you only need to derive a value.",
              "correct": true
            },
            {
              "text": "Model local component state with signals instead of manual change detection.",
              "correct": true
            }
          ],
          "explanation": "computed() must stay pure — deriving a value with no side effects or signal mutations — you should prefer it over an effect() whenever you only need derived state, and you can model local component state with signals instead of manual change detection. Putting side effects or heavy work in a computed breaks its purity and memoization; side effects belong in effect()."
        },
        {
          "questionText": "True or False: Using an effect() to read one signal and set() another signal is the recommended way to create derived state.",
          "options": [
            {
              "text": "True"
            },
            {
              "text": "False",
              "correct": true
            }
          ],
          "explanation": "deriving one signal's value from others is exactly what computed() is for — it is pure, memoized, and updates automatically. Using an effect() to read one signal and set another is an anti-pattern: it invites feedback loops, runs extra work, and Angular disallows writing to signals inside effects by default."
        }
      ]
    },
    {
      "quizId": "component-architecture",
      "milestone": "Component Architecture",
      "summary": "Design scalable Angular apps with clean component structure and state patterns.",
      "image": "assets/images/component-architecture.svg",
      "difficulty": "advanced",
      "facts": [
        "Smart (container) components own data and orchestration, while presentational components stay focused on rendering — a split that keeps UI pieces reusable and testable.",
        "Content projection lets a component host arbitrary template content, often a cleaner design than adding another configurable @Input() for every variation.",
        "Organizing a large codebase by feature rather than by file type keeps related components, services, and routes together and ready to lazy-load."
      ],
      "questions": [
        {
          "questionText": "In a container/presentational split, which responsibility most clearly belongs to the container (smart) component?",
          "options": [
            {
              "text": "Fetching data from a service and deciding what to pass down to child components.",
              "correct": true
            },
            {
              "text": "Applying CSS classes to visually style a button."
            },
            {
              "text": "Rendering a list of items it receives through an @Input()."
            },
            {
              "text": "Emitting a click event up to its parent."
            }
          ],
          "explanation": "Containers own data and orchestration; presentational components just render inputs and emit events — which keeps the UI pieces reusable and testable."
        },
        {
          "questionText": "A component fetches users over HTTP, sorts them, formats dates for display, and renders the table itself. Which principle does this most directly violate?",
          "options": [
            {
              "text": "Single Responsibility Principle",
              "correct": true
            },
            {
              "text": "Dependency Inversion Principle"
            },
            {
              "text": "Liskov Substitution Principle"
            },
            {
              "text": "Open/Closed Principle"
            }
          ],
          "explanation": "One unit, one reason to change: data access belongs in a service, formatting in pipes/helpers, and rendering in the component."
        },
        {
          "questionText": "A presentational (dumb) component should generally avoid fetching its own data directly and instead receive it through inputs. True or False?",
          "options": [
            {
              "text": "True",
              "correct": true
            },
            {
              "text": "False"
            }
          ],
          "explanation": "Receiving data through inputs keeps a presentational component reusable and predictable; fetching its own data couples it to services and hurts testability."
        },
        {
          "questionText": "You're building a reusable Card component whose body can contain arbitrary markup — text, images, even other components. Why is content projection (ng-content) a better fit than exposing many @Input() properties?",
          "options": [
            {
              "text": "Projection lets consumers pass rich, arbitrary template content the Card doesn't need to know about, instead of forcing every variation through predefined inputs.",
              "correct": true
            },
            {
              "text": "Projection renders faster because it skips change detection for the projected content."
            },
            {
              "text": "Inputs cannot pass strings, so projection is the only way to show text."
            },
            {
              "text": "Projection automatically converts the component into a standalone component."
            }
          ],
          "explanation": "Projection lets the consumer supply arbitrary inner content while the wrapper owns only layout — cleaner than an ever-growing list of @Input() flags."
        },
        {
          "questionText": "Which of the following are genuine architectural advantages of standalone components? Select all that apply.",
          "options": [
            {
              "text": "A component declares its own dependencies via its imports, removing the need to hunt through NgModules.",
              "correct": true
            },
            {
              "text": "They can be lazy-loaded directly at the route level without a wrapping feature module.",
              "correct": true
            },
            {
              "text": "They make a component's actual dependencies easier to see and prune.",
              "correct": true
            },
            {
              "text": "They automatically apply the OnPush change detection strategy to every component."
            }
          ],
          "explanation": "Standalone components declare their own imports, lazy-load per route, and expose their real dependencies. They don't change the change-detection strategy — OnPush stays a separate choice."
        },
        {
          "questionText": "Two sibling components need to stay in sync, but neither is the parent of the other. Which approach is the most maintainable?",
          "options": [
            {
              "text": "Share state through an injectable service both siblings consume (e.g. a signal or observable), so neither depends on the other directly.",
              "correct": true
            },
            {
              "text": "Have one sibling directly reference the other via @ViewChild."
            },
            {
              "text": "Store the shared value on the global window object and poll it."
            },
            {
              "text": "Route the data through the DOM by dispatching custom browser events."
            }
          ],
          "explanation": "A shared service is a neutral mediator: both siblings depend on it, not on each other, which keeps them decoupled and testable."
        },
        {
          "questionText": "You need to model synchronous, always-available UI state (like the currently selected tab) that many parts of a template read. Which tool is the most natural architectural fit?",
          "options": [
            {
              "text": "A signal, since it holds a current value synchronously and integrates directly with the template and change detection.",
              "correct": true
            },
            {
              "text": "A Subject, because signals cannot be read from a template."
            },
            {
              "text": "A Promise, because all UI state must be asynchronous."
            },
            {
              "text": "A plain global variable mutated from anywhere in the app."
            }
          ],
          "explanation": "Signals fit synchronous, always-available UI state; RxJS fits asynchronous streams over time. Match the tool to the shape of the data."
        },
        {
          "questionText": "Which architectural choices help avoid unnecessary rendering in a large Angular application? Select all that apply.",
          "options": [
            {
              "text": "Adopting OnPush (or signals) so components re-render only when their inputs actually change.",
              "correct": true
            },
            {
              "text": "Splitting large components into smaller ones so change detection can skip untouched branches.",
              "correct": true
            },
            {
              "text": "Using trackBy / track in loops so Angular reuses DOM nodes instead of re-creating them.",
              "correct": true
            },
            {
              "text": "Disabling change detection globally and calling detectChanges() by hand everywhere."
            }
          ],
          "explanation": "OnPush/signals narrow when checks run, smaller components let Angular skip subtrees, and track/trackBy avoids DOM churn. Manually disabling change detection everywhere is a fragile anti-pattern."
        },
        {
          "questionText": "Large Angular applications are generally more maintainable when organized primarily by feature (domain) rather than by file type. True or False?",
          "options": [
            {
              "text": "True",
              "correct": true
            },
            {
              "text": "False"
            }
          ],
          "explanation": "Feature folders keep a capability's files together so changes stay local and features can lazy-load; organizing by file type scatters each feature and scales poorly."
        },
        {
          "questionText": "Which practices best promote reuse and reduce duplication across components? Select all that apply.",
          "options": [
            {
              "text": "Favoring composition (small components and directives combined) over deep inheritance hierarchies.",
              "correct": true
            },
            {
              "text": "Extracting shared behavior into directives or services instead of copy-pasting logic.",
              "correct": true
            },
            {
              "text": "Designing presentational components to depend only on inputs and outputs, not on specific services.",
              "correct": true
            },
            {
              "text": "Adding a feature-specific @Input() flag to a shared component for every new screen that uses it."
            }
          ],
          "explanation": "Reuse comes from composition, shared directives/services, and presentational components decoupled from data sources — not from piling screen-specific flags onto a shared component."
        }
      ]
    },
    {
      "quizId": "dependency-injection-advanced",
      "milestone": "Advanced DI",
      "summary": "Master Angular's DI system: injectors, providers, and smart scoping choices.",
      "image": "assets/images/dependency-injection-advanced.svg",
      "difficulty": "advanced",
      "facts": [
        "Angular has two parallel injector trees: the EnvironmentInjector hierarchy (platform to root to route) and the ElementInjector hierarchy that follows the component tree.",
        "A class is its own DI token, which is why non-class dependencies (config objects, primitives, interfaces) need an InjectionToken to be injected.",
        "providedIn: 'root' services are tree-shakable: if nothing injects a service, the bundler can drop it from the build."
      ],
      "questions": [
        {
          "questionText": "Dependency Injection is Angular's implementation of Inversion of Control. What is the primary architectural benefit it provides?",
          "options": [
            {
              "text": "A class declares the dependencies it needs and Angular supplies them, so the class isn't coupled to how they're created.",
              "correct": true
            },
            {
              "text": "It makes every service a global variable accessible from anywhere."
            },
            {
              "text": "It automatically converts synchronous code into asynchronous code."
            },
            {
              "text": "It removes the need to write any services."
            }
          ],
          "explanation": "Inversion of Control means a class no longer constructs its own dependencies; it declares what it needs and the injector supplies them. This decouples the class from concrete implementations, so you can swap or mock dependencies and test it in isolation."
        },
        {
          "questionText": "In a modern Angular app, when is the inject() function especially useful compared to constructor parameter injection?",
          "options": [
            {
              "text": "When you need a dependency outside a constructor, such as in a field initializer, a functional guard/interceptor, or a reusable helper.",
              "correct": true
            },
            {
              "text": "Only inside HTML templates."
            },
            {
              "text": "When you want to bypass the injector entirely."
            },
            {
              "text": "inject() and constructor injection can never be used in the same class."
            }
          ],
          "explanation": "inject() resolves a dependency from the current injection context without a constructor parameter, which makes it ideal for field initializers, functional route guards/interceptors, and shared helper functions. Both styles use the same injector."
        },
        {
          "questionText": "Because dependencies are injected rather than created inside a class, you can replace a real service with a mock in tests without modifying the class under test. True or False?",
          "options": [
            {
              "text": "True",
              "correct": true
            },
            {
              "text": "False"
            }
          ],
          "explanation": "Injecting dependencies means the class receives them from outside, so a test can supply a fake implementation through the injector while the class stays unchanged. This is a core reason DI improves testability and keeps units loosely coupled."
        },
        {
          "questionText": "You want the token LoggerService to resolve to an already-registered ConsoleLoggerService (the same singleton, not a new instance). Which provider recipe fits?",
          "options": [
            {
              "text": "useExisting, which aliases the token to an existing provider so both share one instance.",
              "correct": true
            },
            {
              "text": "useClass, which creates a separate new instance of the class."
            },
            {
              "text": "useValue, which provides a fixed static value."
            },
            {
              "text": "useFactory, which runs a function to build a value."
            }
          ],
          "explanation": "useExisting aliases one token to another, so both resolve to the same single instance, which is useful for exposing a service under an interface token. useClass would create a distinct second instance, useValue supplies a static value, and useFactory constructs one via a function."
        },
        {
          "questionText": "When is an InjectionToken the right choice for a dependency? Select all that apply.",
          "options": [
            {
              "text": "For non-class values like configuration objects, base URLs, or feature flags.",
              "correct": true
            },
            {
              "text": "When you need a strongly-typed key for something that has no class to reference.",
              "correct": true
            },
            {
              "text": "To provide an interface, which has no runtime representation to inject by.",
              "correct": true
            },
            {
              "text": "Whenever you inject any ordinary service class."
            }
          ],
          "explanation": "Classes act as their own DI tokens, but interfaces, primitives, and config objects don't exist at runtime, so there is nothing to inject by. An InjectionToken creates a unique, strongly-typed key for those non-class dependencies. Injecting a normal service class needs no token."
        },
        {
          "questionText": "A service holds state that must be unique per instance of a particular component (each component instance gets its own copy). Where should it be provided?",
          "options": [
            {
              "text": "In that component's providers array, so each component instance gets its own service instance.",
              "correct": true
            },
            {
              "text": "In providedIn: 'root', so it is a global singleton."
            },
            {
              "text": "In a shared module imported everywhere."
            },
            {
              "text": "As a useValue provider at the root."
            }
          ],
          "explanation": "Providing a service in a component's providers array creates an instance tied to that component's element injector, so each component instance (and its children) gets its own copy that is destroyed with the component. providedIn:'root' would instead share one global singleton across the whole app."
        },
        {
          "questionText": "Which statements about DI resolution modifiers are correct? Select all that apply.",
          "options": [
            {
              "text": "@Optional lets a dependency resolve to null instead of throwing when no provider is found.",
              "correct": true
            },
            {
              "text": "@SkipSelf starts the lookup at the parent injector, skipping the current one.",
              "correct": true
            },
            {
              "text": "@Self restricts resolution to the current injector only, failing if it is not provided there.",
              "correct": true
            },
            {
              "text": "@Host makes the dependency available to every injector in the application."
            }
          ],
          "explanation": "@Optional turns a missing dependency into null rather than an error, @SkipSelf skips the local injector and resolves from an ancestor, and @Self limits lookup to the current injector. @Host stops the search at the host component instead of broadening availability. Each modifier controls where resolution starts or stops."
        },
        {
          "questionText": "At startup you must build a service differently depending on environment or config values loaded at runtime. Which provider recipe is designed for this?",
          "options": [
            {
              "text": "useFactory, whose function receives injected dependencies and returns the correctly-configured instance.",
              "correct": true
            },
            {
              "text": "useValue, which can only supply a static, pre-known value."
            },
            {
              "text": "useExisting, which only aliases an existing token."
            },
            {
              "text": "useClass, which always constructs the class the same way."
            }
          ],
          "explanation": "useFactory runs a function (with its own injected deps) to construct the value, so it can branch on runtime configuration or environment data to build the right instance. useValue is static, useClass is fixed construction, and useExisting only aliases another token."
        },
        {
          "questionText": "Which practices lead to a healthier DI setup in a large app? Select all that apply.",
          "options": [
            {
              "text": "Keep services focused on a single responsibility to avoid bloated, do-everything services.",
              "correct": true
            },
            {
              "text": "Depend on abstractions or tokens where it aids testing and swapping implementations.",
              "correct": true
            },
            {
              "text": "Break circular dependencies by extracting shared logic or rethinking responsibilities.",
              "correct": true
            },
            {
              "text": "Provide every service at the component level to be safe."
            }
          ],
          "explanation": "Small, single-responsibility services stay testable and reusable, depending on tokens or abstractions lets you swap and mock implementations, and circular dependencies signal tangled responsibilities that should be refactored. Blanket component-level provisioning needlessly multiplies instances and memory; scope should match actual need."
        },
        {
          "questionText": "A service declared with providedIn: 'root' is a single app-wide instance and can be tree-shaken away if nothing injects it. True or False?",
          "options": [
            {
              "text": "True",
              "correct": true
            },
            {
              "text": "False"
            }
          ],
          "explanation": "providedIn:'root' registers the service with the root injector as one lazily-created singleton, and because the provider is declared on the service itself, the bundler can drop it entirely when it is never injected. This is why providedIn:'root' is the recommended default scope for most services."
        }
      ]
    },
    {
      "quizId": "directives",
      "milestone": "Directives",
      "summary": "Use Angular directives and control flow to build clean, dynamic templates.",
      "image": "assets/images/directives.svg",
      "difficulty": "beginner",
      "facts": [
        "Angular has three directive types: components (a directive with a template), structural directives that change DOM layout, and attribute directives that change an element's look or behavior.",
        "The newer control-flow blocks (@if, @for, @switch) are built into the template syntax and replace the older *ngIf/*ngFor/*ngSwitch structural directives.",
        "ng-container groups elements and hosts structural logic without rendering any element of its own, which keeps the DOM clean."
      ],
      "questions": [
        {
          "questionText": "Angular has three kinds of directives. Which trio is correct?",
          "options": [
            {
              "text": "Components, structural directives, and attribute directives.",
              "correct": true
            },
            {
              "text": "Services, pipes, and guards."
            },
            {
              "text": "Modules, components, and templates."
            },
            {
              "text": "Inputs, outputs, and lifecycle hooks."
            }
          ],
          "explanation": "Directives add behavior to elements. Angular has three types: components (a directive with a template), structural directives that change DOM layout by adding or removing elements, and attribute directives that change an existing element's appearance or behavior. Services, pipes, and guards play different roles."
        },
        {
          "questionText": "What is the key difference between a structural directive and an attribute directive?",
          "options": [
            {
              "text": "A structural directive adds or removes elements from the DOM, while an attribute directive changes an existing element's look or behavior.",
              "correct": true
            },
            {
              "text": "A structural directive only works on a <div>, while an attribute directive works anywhere."
            },
            {
              "text": "Structural directives run on the server and attribute directives run in the browser."
            },
            {
              "text": "There is no real difference; the names are interchangeable."
            }
          ],
          "explanation": "Structural directives (like @if and @for) reshape the DOM by creating or destroying elements, while attribute directives (like ngClass and ngStyle) leave the structure intact and modify an existing element. Deciding whether you need to restructure or restyle points you to the right tool."
        },
        {
          "questionText": "Structural directives can add or remove elements from the DOM. True or False?",
          "options": [
            {
              "text": "True",
              "correct": true
            },
            {
              "text": "False"
            }
          ],
          "explanation": "That is what makes them 'structural': directives like @if and @for change the structure of the page by inserting or removing elements based on your data, rather than only restyling what is already there."
        },
        {
          "questionText": "You want a 'Welcome back' banner to appear only when a user is logged in. Which control-flow block fits?",
          "options": [
            {
              "text": "@if, which renders its content only when the condition is true and removes it otherwise.",
              "correct": true
            },
            {
              "text": "@for, which repeats content for each item in a collection."
            },
            {
              "text": "@switch, which picks one branch from several based on a value."
            },
            {
              "text": "ngStyle, which sets inline styles on an element."
            }
          ],
          "explanation": "@if conditionally includes or excludes a block based on a boolean. When the user is logged out the banner isn't merely hidden, it isn't in the DOM at all, which is cleaner and lighter than rendering it and toggling visibility."
        },
        {
          "questionText": "Which statements about the @for block are correct? Select all that apply.",
          "options": [
            {
              "text": "It renders a piece of template once for each item in a collection.",
              "correct": true
            },
            {
              "text": "Its track expression helps Angular identify items so it can reuse DOM nodes efficiently.",
              "correct": true
            },
            {
              "text": "It can provide an @empty block to show when the collection has no items.",
              "correct": true
            },
            {
              "text": "It is the right choice for showing a single element based on a boolean."
            }
          ],
          "explanation": "@for repeats template content across a collection, uses a track expression so Angular can match items to existing DOM nodes and avoid needless re-creation, and offers an @empty block for the no-items case. Showing a single element on a boolean is @if's job."
        },
        {
          "questionText": "You need to apply a CSS class only when a form field is invalid. Which is the most appropriate tool?",
          "options": [
            {
              "text": "ngClass, which toggles one or more CSS classes based on a condition.",
              "correct": true
            },
            {
              "text": "@for, which repeats an element per item."
            },
            {
              "text": "ng-container, which groups elements without adding DOM."
            },
            {
              "text": "A template reference variable."
            }
          ],
          "explanation": "ngClass is an attribute directive that adds or removes classes from an expression, such as an 'invalid' class when a control fails validation. It changes appearance without altering DOM structure, which is exactly an attribute directive's role."
        },
        {
          "questionText": "You want to apply an @if to a group of elements without wrapping them in an extra &lt;div&gt; that would appear in the DOM. What should you use?",
          "options": [
            {
              "text": "ng-container, a grouping element that applies structural logic but renders no element of its own.",
              "correct": true
            },
            {
              "text": "ng-template, a block that is only rendered when explicitly referenced."
            },
            {
              "text": "An extra <div> with display:none."
            },
            {
              "text": "ngStyle on each element."
            }
          ],
          "explanation": "ng-container is an invisible grouping wrapper: it lets you attach structural control flow to several elements at once without emitting a real DOM node. This keeps the rendered markup clean and avoids stray wrappers that can break layouts or CSS."
        },
        {
          "questionText": "When is creating a custom attribute directive a good choice? Select all that apply.",
          "options": [
            {
              "text": "To reuse the same DOM behavior, such as a highlight-on-hover effect, across many elements.",
              "correct": true
            },
            {
              "text": "To encapsulate small, cross-cutting behavior so components stay simple.",
              "correct": true
            },
            {
              "text": "To keep repeated logic in one place instead of copying it into every component.",
              "correct": true
            },
            {
              "text": "Whenever you need to display a completely new section of UI with its own template."
            }
          ],
          "explanation": "Custom attribute directives shine for reusable, cross-cutting behavior attached to existing elements (highlighting, autofocus, tracking), keeping that logic in one place. When you instead need a new piece of UI with its own template, that is a component's job."
        },
        {
          "questionText": "In which scenarios is an attribute directive (rather than a structural one) the natural choice? Select all that apply.",
          "options": [
            {
              "text": "Highlighting a field's border red when validation fails.",
              "correct": true
            },
            {
              "text": "Applying a 'dark-mode' class to an element based on a setting.",
              "correct": true
            },
            {
              "text": "Changing a button's style when it is disabled.",
              "correct": true
            },
            {
              "text": "Removing a section of the page entirely when a feature flag is off."
            }
          ],
          "explanation": "Attribute directives change how an existing element looks or behaves (error styling, theme classes, disabled-state styles) without touching structure. Removing a whole section based on a flag changes structure, so that is a structural directive (@if)."
        },
        {
          "questionText": "Attribute directives typically modify the appearance or behavior of an existing element rather than adding or removing elements. True or False?",
          "options": [
            {
              "text": "True",
              "correct": true
            },
            {
              "text": "False"
            }
          ],
          "explanation": "Attribute directives (ngClass, ngStyle, custom highlight directives) operate on an element already in the DOM, adjusting its classes, styles, or behavior. Adding or removing elements is the job of structural directives, and separating these responsibilities is a core directive concept."
        }
      ]
    },
    {
      "quizId": "pipes",
      "milestone": "Pipes",
      "summary": "Transform and format template data with Angular's built-in and custom pipes.",
      "image": "assets/images/pipes.svg",
      "difficulty": "beginner",
      "facts": [
        "A pipe transforms a value for display using the | symbol in a template, without changing the original data.",
        "The AsyncPipe subscribes to an Observable and unsubscribes automatically when the component is destroyed, preventing memory leaks.",
        "Chained pipes run left to right, so {{ value | slice:0:5 | uppercase }} slices first, then uppercases the result."
      ],
      "questions": [
        {
          "questionText": "What is the main purpose of a pipe in an Angular template?",
          "options": [
            {
              "text": "To make HTTP requests directly from the template."
            },
            {
              "text": "To define routing between pages."
            },
            {
              "text": "To transform or format a value for display without changing the underlying data.",
              "correct": true
            },
            {
              "text": "To inject services into a component."
            }
          ],
          "explanation": "A pipe takes an input value and returns a transformed version for display (formatting a date, uppercasing text, showing a price) while leaving the original data untouched. This keeps formatting in the template and out of your component logic."
        },
        {
          "questionText": "Pipes are primarily used to transform data for display in templates. True or False?",
          "options": [
            {
              "text": "True",
              "correct": true
            },
            {
              "text": "False"
            }
          ],
          "explanation": "Pipes exist to format and transform values right where they are shown, so the same underlying data can be presented in different ways without duplicating formatting logic across components."
        },
        {
          "questionText": "Which built-in pipe converts a string to all uppercase letters for display?",
          "options": [
            {
              "text": "UpperCasePipe, e.g. {{ name | uppercase }}.",
              "correct": true
            },
            {
              "text": "DecimalPipe."
            },
            {
              "text": "TitleCasePipe."
            },
            {
              "text": "SlicePipe."
            }
          ],
          "explanation": "UpperCasePipe transforms text to all caps for display. TitleCasePipe capitalizes the first letter of each word, SlicePipe returns a subset of an array or string, and DecimalPipe formats numbers, so each built-in pipe has a specific formatting job."
        },
        {
          "questionText": "You have a raw Date value and want to show it as 'Jul 17, 2026'. Which pipe is designed for this?",
          "options": [
            {
              "text": "JsonPipe, which serializes a value to JSON."
            },
            {
              "text": "CurrencyPipe, which formats monetary values."
            },
            {
              "text": "SlicePipe, which extracts part of a collection."
            },
            {
              "text": "DatePipe, which formats dates using a format string and locale.",
              "correct": true
            }
          ],
          "explanation": "DatePipe formats date values into human-readable strings using a format pattern (like 'mediumDate') and respects locale. It is the standard way to present dates without writing manual date-formatting code in the component."
        },
        {
          "questionText": "Which built-in pipes are used to format numeric values? Select all that apply.",
          "options": [
            {
              "text": "PercentPipe, which formats a number as a percentage such as 42%.",
              "correct": true
            },
            {
              "text": "JsonPipe, which formats a number into a currency symbol."
            },
            {
              "text": "CurrencyPipe, which formats a number as a monetary amount such as $1,299.00.",
              "correct": true
            },
            {
              "text": "DecimalPipe, which formats a number with grouping and a set number of decimals.",
              "correct": true
            }
          ],
          "explanation": "CurrencyPipe, DecimalPipe, and PercentPipe all format numbers for display, as money, grouped decimals, and percentages respectively. JsonPipe is unrelated to number formatting; it serializes any value to a JSON string, mainly for debugging."
        },
        {
          "questionText": "While debugging, you want to see the full contents of an object directly in the template. Which pipe helps?",
          "options": [
            {
              "text": "UpperCasePipe."
            },
            {
              "text": "JsonPipe, e.g. {{ user | json }}, which serializes the value to a readable JSON string.",
              "correct": true
            },
            {
              "text": "DatePipe."
            },
            {
              "text": "AsyncPipe."
            }
          ],
          "explanation": "JsonPipe converts an object or array into its JSON string form so you can inspect its shape and values right in the template. It is a handy debugging aid, not something you would typically ship in a polished UI."
        },
        {
          "questionText": "What does the AsyncPipe do when you write {{ data$ | async }} in a template?",
          "options": [
            {
              "text": "It converts an Observable into a Promise."
            },
            {
              "text": "It makes an HTTP request each time the template renders."
            },
            {
              "text": "It subscribes to the Observable, displays the latest emitted value, and unsubscribes automatically when the component is destroyed.",
              "correct": true
            },
            {
              "text": "It caches the Observable to localStorage."
            }
          ],
          "explanation": "AsyncPipe subscribes to an Observable (or Promise), renders its most recent value, and cleans up the subscription when the component is destroyed. This removes manual subscribe/unsubscribe boilerplate and prevents memory leaks, which is why it is the recommended way to display async data."
        },
        {
          "questionText": "Which statements about chaining pipes, e.g. {{ value | pipeA | pipeB }}, are correct? Select all that apply.",
          "options": [
            {
              "text": "It can keep templates readable when each pipe does one clear job.",
              "correct": true
            },
            {
              "text": "The order of chained pipes never affects the final result."
            },
            {
              "text": "Pipes are applied left to right, so pipeA runs first and its result is passed to pipeB.",
              "correct": true
            },
            {
              "text": "Chaining lets you combine simple transformations instead of writing one complex expression.",
              "correct": true
            }
          ],
          "explanation": "Chained pipes evaluate left to right, feeding each result into the next, so order matters (slicing then uppercasing differs from uppercasing then slicing). Chaining small, single-purpose pipes keeps a transformation readable rather than cramming logic into one expression."
        },
        {
          "questionText": "The AsyncPipe automatically unsubscribes from an Observable when the component is destroyed. True or False?",
          "options": [
            {
              "text": "True",
              "correct": true
            },
            {
              "text": "False"
            }
          ],
          "explanation": "AsyncPipe manages the subscription lifecycle: it subscribes when rendering and unsubscribes on destroy. That automatic cleanup is a major reason it is preferred over manually subscribing in the component, which risks memory leaks if you forget to unsubscribe."
        },
        {
          "questionText": "Which are good reasons to use or create a pipe? Select all that apply.",
          "options": [
            {
              "text": "To reuse the same transformation across many templates via a custom pipe.",
              "correct": true
            },
            {
              "text": "To present data cleanly without mutating the underlying value.",
              "correct": true
            },
            {
              "text": "To perform heavy business logic or data fetching during rendering."
            },
            {
              "text": "To keep formatting logic in the template and out of component code.",
              "correct": true
            }
          ],
          "explanation": "Pipes are for lightweight, reusable formatting that leaves source data unchanged and keeps templates clean, and a custom pipe centralizes a repeated transformation. They should stay side-effect free; heavy business logic or data fetching belongs in services or components, not in a pipe that runs during rendering."
        }
      ]
    },
    {
      "quizId": "design-patterns",
      "milestone": "Design Patterns",
      "summary": "Apply proven design patterns to build scalable, maintainable Angular architecture.",
      "image": "assets/images/design-patterns.svg",
      "difficulty": "advanced",
      "facts": [
        "Design patterns are shared, proven solutions to recurring problems; they give a team a common vocabulary and a maintainable structure, not a performance boost.",
        "A Facade service coordinates several underlying services behind one simple API, so components depend on the facade instead of the tangle beneath it.",
        "RxJS Observables and Subjects are Angular’s built-in expression of the Observer pattern: subscribers react to values emitted over time."
      ],
      "questions": [
        {
          "questionText": "What problem do software design patterns primarily solve?",
          "options": [
            {
              "text": "They make an application run faster by optimizing the compiler at runtime."
            },
            {
              "text": "They provide proven, reusable solutions to recurring design problems, improving maintainability and communication.",
              "correct": true
            },
            {
              "text": "They eliminate the need to write tests."
            },
            {
              "text": "They guarantee an application will never contain bugs."
            }
          ],
          "explanation": "Design patterns capture battle-tested approaches to problems that recur across projects. They don't speed up runtime or remove bugs; their value is a shared vocabulary and a maintainable structure that makes systems easier to reason about, extend, and discuss across a team."
        },
        {
          "questionText": "Composition is generally preferred over inheritance when designing Angular components. True or False?",
          "options": [
            {
              "text": "True",
              "correct": true
            },
            {
              "text": "False"
            }
          ],
          "explanation": "Composition, building behavior by combining small components, directives, and services, keeps pieces loosely coupled and independently reusable. Deep inheritance hierarchies tightly bind subclasses to base-class decisions and grow brittle. Angular's model (inputs, outputs, content projection, DI) is built around composition, which scales better."
        },
        {
          "questionText": "An architecture that separates data-fetching 'container' components from stateless 'presentational' components mainly improves what?",
          "options": [
            {
              "text": "Bundle size, by removing all services."
            },
            {
              "text": "Runtime speed, by disabling change detection."
            },
            {
              "text": "Type safety, by removing interfaces."
            },
            {
              "text": "Maintainability and testability, by isolating business and data logic from pure rendering.",
              "correct": true
            }
          ],
          "explanation": "Splitting smart (data and orchestration) from presentational (rendering) components concentrates business logic where it can be tested without a DOM and keeps UI pieces reusable and predictable. It's a separation-of-concerns win, not a bundle-size or change-detection mechanism."
        },
        {
          "questionText": "A component needs data that requires coordinating three services with intricate logic. Which pattern gives the component a single, clean API and hides that complexity?",
          "options": [
            {
              "text": "The Facade pattern, a service that exposes a simple interface and orchestrates the underlying services internally.",
              "correct": true
            },
            {
              "text": "The Adapter pattern, converting one interface into another."
            },
            {
              "text": "The Strategy pattern, swapping interchangeable algorithms."
            },
            {
              "text": "The Factory pattern, centralizing object creation."
            }
          ],
          "explanation": "A Facade service wraps several collaborating services behind one simple, intention-revealing API. Components depend only on the facade, not on the tangle of services beneath it, which reduces coupling and component complexity and keeps the orchestration easy to change or test in one place."
        },
        {
          "questionText": "In which situations is the Strategy pattern a natural fit? Select all that apply.",
          "options": [
            {
              "text": "Selecting one of several interchangeable validation rules at runtime.",
              "correct": true
            },
            {
              "text": "Converting a third-party library's data shape into your app's model."
            },
            {
              "text": "Supporting multiple sorting or pricing algorithms chosen by configuration.",
              "correct": true
            },
            {
              "text": "Swapping a behavior at runtime without changing the code that uses it.",
              "correct": true
            }
          ],
          "explanation": "Strategy encapsulates a family of interchangeable behaviors behind a common interface so the consumer can pick one at runtime without branching logic. Validation rules, sorting, and pricing are classic cases. Converting an incompatible data shape is the Adapter pattern, not Strategy."
        },
        {
          "questionText": "You must integrate a legacy REST API whose response shape doesn't match your app's models, and you want to isolate that mismatch. Which pattern applies?",
          "options": [
            {
              "text": "The Observer pattern, reacting to emitted events over time."
            },
            {
              "text": "The Facade pattern, simplifying your own internal services."
            },
            {
              "text": "The Adapter pattern, a layer that converts the external interface into the shape your app expects.",
              "correct": true
            },
            {
              "text": "The Singleton pattern, ensuring a single instance."
            }
          ],
          "explanation": "An Adapter wraps an incompatible external interface and translates it into the shape your app expects, keeping the mismatch in one isolated place. If the API changes, only the adapter changes while your components and models stay stable, which isolates external dependencies."
        },
        {
          "questionText": "Angular's use of RxJS Observables and Subjects for event-driven, reactive communication is an example of which pattern?",
          "options": [
            {
              "text": "The Adapter pattern."
            },
            {
              "text": "The Observer pattern, where subscribers react to values a subject emits over time.",
              "correct": true
            },
            {
              "text": "The Facade pattern."
            },
            {
              "text": "The Template Method pattern."
            }
          ],
          "explanation": "Observables and Subjects implement the Observer pattern: producers emit values and any number of subscribers react as those values arrive. This decouples the source of events from their consumers and underpins Angular’s reactive, event-driven communication."
        },
        {
          "questionText": "When deciding WHERE application state should live, which guidelines hold? Select all that apply.",
          "options": [
            {
              "text": "All state should live in one global store regardless of scope, for consistency."
            },
            {
              "text": "State used by only one component should stay local to that component.",
              "correct": true
            },
            {
              "text": "State shared by several components is better held in a service, for example via a signal or observable.",
              "correct": true
            },
            {
              "text": "State scope should match how widely it is actually needed, not be global by default.",
              "correct": true
            }
          ],
          "explanation": "State should live at the narrowest scope that needs it: local for single-component state, a shared service for cross-component state. Defaulting everything to a global store adds coupling and complexity you may not need. Right-sizing state scope is a core architectural decision."
        },
        {
          "questionText": "The Adapter pattern's primary purpose is to create new objects at runtime based on configuration. True or False?",
          "options": [
            {
              "text": "True"
            },
            {
              "text": "False",
              "correct": true
            }
          ],
          "explanation": "That describes the Factory pattern, not the Adapter. The Adapter's purpose is to convert one interface into another so incompatible components can work together, such as wrapping a third-party API. Distinguishing create versus convert is key to choosing the right pattern."
        },
        {
          "questionText": "You're building an extensible checkout that must support multiple payment providers added over time. Which approaches fit well? Select all that apply.",
          "options": [
            {
              "text": "Define a common payment interface and select the provider's implementation at runtime (Strategy).",
              "correct": true
            },
            {
              "text": "Wrap each third-party provider SDK behind an adapter so the app depends on your interface, not theirs (Adapter).",
              "correct": true
            },
            {
              "text": "Hard-code a large if/else per provider throughout the checkout components."
            },
            {
              "text": "Expose a single PaymentFacade so components don't coordinate providers directly (Facade).",
              "correct": true
            }
          ],
          "explanation": "Extensibility comes from programming to interfaces: Strategy to swap provider implementations, Adapter to isolate each third-party SDK, and a Facade to give components one clean entry point. Scattering provider-specific if/else through components is the rigid anti-pattern these patterns solve."
        }
      ]
    }
  ],
  "resources": [
    {
      "quizId": "TS_Quiz",
      "milestone": "TypeScript",
      "resources": [
        {
          "title": "TypeScript language website",
          "url": "https://www.typescriptlang.org",
          "host": "TypeScript website"
        },
        {
          "title": "Microsoft TypeScript GitHub page",
          "url": "https://github.com/microsoft/TypeScript",
          "host": "GitHub"
        },
        {
          "title": "TypeScript Wiki",
          "url": "https://en.wikipedia.org/wiki/TypeScript",
          "host": "Wikipedia"
        },
        {
          "title": "TypeScript blog",
          "url": "https://devblogs.microsoft.com/typescript/",
          "host": "Microsoft dev blogs"
        }
      ]
    },
    {
      "quizId": "create-first-app",
      "milestone": "Creating your first app",
      "resources": [
        {
          "title": "Getting started with a basic Angular app",
          "url": "https://angular.io/start",
          "host": "angular.io"
        },
        {
          "title": "Creating Your First Angular App: Basics",
          "url": "https://code.tutsplus.com/tutorials/creating-your-first-angular-app-basics--cms-30092",
          "host": "envatotuts+"
        }
      ]
    },
    {
      "quizId": "templates",
      "milestone": "Templates",
      "resources": [
        {
          "title": "Introduction to components and templates",
          "url": "https://angular.io/guide/architecture-components#:~:text=Angular%20templates%20are%20dynamic.,component%20is%20technically%20a%20directive.",
          "host": "angular.io"
        },
        {
          "title": "Angular 4 - Templates",
          "url": "https://www.tutorialspoint.com/angular4/angular4_templates.htm",
          "host": "tutorialspoint"
        },
        {
          "title": "Angular templates and views",
          "url": "https://howtodoinjava.com/angular/angular-templates-and-views/#:~:text=A%20template%20is%20an%20HTML,component%20defines%20that%20component's%20view.",
          "host": "HowToDoInJava"
        }
      ]
    },
    {
      "quizId": "dependency-injection",
      "milestone": "Dependency Injection",
      "resources": [
        {
          "title": "Dependency injection in Angular",
          "url": "https://angular.io/guide/dependency-injection",
          "host": "angular.io"
        },
        {
          "title": "Dependency injection in action",
          "url": "https://angular.io/guide/dependency-injection-in-action",
          "host": "angular.io"
        },
        {
          "title": "Introduction to services and dependency injection",
          "url": "https://angular.io/guide/architecture-services",
          "host": "angular.io"
        },
        {
          "title": "Total Guide To Angular 6+ Dependency Injection...",
          "url": "https://medium.com/@tomastrajan/total-guide-to-angular-6-dependency-injection-providedin-vs-providers-85b7a347b59f",
          "host": "medium.com"
        }
      ]
    },
    {
      "quizId": "component-tree",
      "milestone": "Component Trees",
      "resources": [
        {
          "title": "Introduction to Angular concepts",
          "url": "https://angular.io/guide/architecture",
          "host": "angular.io"
        },
        {
          "title": "The Core Concepts of Angular",
          "url": "https://vsavkin.com/the-core-concepts-of-angular-2-c3d6cbe04d04",
          "host": "vsavkin.com"
        }
      ]
    },
    {
      "quizId": "router",
      "milestone": "Angular Router",
      "resources": [
        {
          "title": "In-app navigation: routing to views",
          "url": "https://angular.io/guide/router",
          "host": "angular.io"
        },
        {
          "title": "API: @angular/router",
          "url": "https://angular.io/api/router",
          "host": "angular.io"
        },
        {
          "title": "A Complete Guide To Routing In Angular",
          "url": "https://www.smashingmagazine.com/2018/11/a-complete-guide-to-routing-in-angular/",
          "host": "Smashing Magazine"
        },
        {
          "title": "Using Route Parameters",
          "url": "https://angular-2-training-book.rangle.io/routing/routeparams",
          "host": "Rangle.io"
        }
      ]
    },
    {
      "quizId": "material",
      "milestone": "Angular Material",
      "resources": [
        {
          "title": "Getting Started with Angular Material",
          "url": "https://material.angular.io/guide/getting-started",
          "host": "Angular Material official website"
        },
        {
          "title": "Angular Material UI component library",
          "url": "https://material.angular.io",
          "host": "Angular Material official website"
        },
        {
          "title": "Material Design",
          "url": "https://www.material.io",
          "host": "material.io"
        },
        {
          "title": "Angular Material 7 Tutorial",
          "url": "https://www.tutorialspoint.com/angular_material7/index.htm",
          "host": "tutorialspoint"
        }
      ]
    },
    {
      "quizId": "forms",
      "milestone": "Angular Forms",
      "resources": [
        {
          "title": "Introduction to forms in Angular",
          "url": "https://angular.io/guide/forms-overview",
          "host": "angular.io"
        },
        {
          "title": "Angular Forms Guide - Template Driven and Reactive Forms",
          "url": "https://blog.angular-university.io/introduction-to-angular-2-forms-template-driven-vs-model-driven/",
          "host": "Angular University"
        },
        {
          "title": "Template-Driven Form Validation In Angular",
          "url": "https://ankitsharmablogs.com/template-driven-form-validation-in-angular/",
          "host": "Ankit Sharma's Blog"
        },
        {
          "title": "How to validate Angular Reactive Forms",
          "url": "https://www.freecodecamp.org/news/how-to-validate-angular-reactive-forms/",
          "host": "freecodecamp.org"
        }
      ]
    },
    {
      "quizId": "angular-cli",
      "milestone": "Angular-CLI",
      "resources": [
        {
          "title": "CLI Overview and Command Reference",
          "url": "https://angular.io/cli",
          "host": "angular.io"
        },
        {
          "title": "Angular 8 Tutorial: Build your First Angular App",
          "url": "https://www.techiediaries.com/angular-8-tutorial-build-first-angular-calculator-app/",
          "host": "TechieDiaries"
        },
        {
          "title": "Starting an Angular app with the Angular CLI",
          "url": "https://scotch.io/courses/build-your-first-angular-website/starting-an-angular-app-with-the-angular-cli",
          "host": "Scotch"
        }
      ]
    }
  ]
}